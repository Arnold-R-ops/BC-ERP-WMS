# P2 补充业务闭环验收

## 2026-07-28 当前结论

整体结论：**通过**。本段记录的是 2026-07-28 验收时状态；采购单创建后的二次编辑已于 2026-08-09 完成，详见 `PURCHASE_ORDER_EDIT_ACCEPTANCE.md`。

- Shopify 消费者事务边界与真实只读同步已通过；不回写 Shopify 是产品边界，不是缺陷。
- 运单新增、修改、作废及审计展示已通过。
- 采购 3/20/50 商品已完成真实网页版替代验收：`PO-20260727-001/002/003` 均为 `ORDERING`、实收 0；用户另建 `PO-20260727-004` 完成 3 商品操作。验收前后库存流水均为 214 条、最大 ID 218。
- 2026-07-28 物理手机已新建 `PO-20260728-001`（ID 63）：3 个不同 SKU、总数量 3、总额 30，状态 `ORDERING`、实收 0；手机搜索、键盘选择、逐行编辑、保存和金额汇总通过，库存流水保持 214 条、最大 ID 218。
- 2026-07-28 物理手机已新建 `PO-20260728-002`（ID 64）：20 个不同 SKU、总数量 20、总额 200，状态 `ORDERING`、实收 0；纵向/横向滚动、键盘搜索、选中保持、逐行编辑、保存和金额汇总通过，库存流水保持 214 条、最大 ID 218。
- 2026-07-28 物理手机已新建 `PO-20260728-003`（ID 65）：50 行、50 个不同 SKU（ID 1～50）、总数量 50、总额 500，状态 `ORDERING`、实收 0；未生成批次，关联库存流水 0，全库流水保持 214 条、最大 ID 218。
- 采购单 56/57/58/59/60/61/62/63/64/65 全部保留，不得确认 ASN 或收货。
- 物理手机 50 商品的新建、分页、表单内逐行编辑、保存和金额汇总已通过；创建后数据库与库存安全核对通过。
- 2026-07-28 验收时系统尚不支持对已创建的 `ORDERING` 采购单再次编辑；该能力已于 2026-08-09 独立完成。“确认 ASN”仍是状态流转操作，不得当作保存或编辑。

## 2026-07-26 Shopify 消费者事务边界修复复验

Shopify 分项结论：**通过**。补充业务闭环已于 2026-07-28 随采购 50 商品物理手机验证关闭并通过；采购单创建后再次编辑已于 2026-08-09 完成。

- 新增明确的 `ShopifyConsumerIdentityService`，通过独立 `REQUIRES_NEW` 工作事务提交消费者身份；没有给整个 `ShopifyCustomerResolver` 直接添加事务注解。
- 订单履约事务继续负责 SKU 解析和待审批销售订单创建。未知 SKU 只保留失败原始事件并新增/更新待映射记录，不创建销售订单、预留、出库任务或库存流水。
- 5 个 Shopify 定向测试类共 30 项通过，包括：新身份随外层事务回滚仍保留、既有身份复用、已知 SKU 建待审批订单、`retailMode=false` 禁止创建、无锚点/模糊身份禁止猜测、6 路并发只生成一条消费者。
- 真实店铺同步两轮均为 `success=0 / skipped=0 / failed=2`。第一轮消费者 `0→2`，第二轮仍为 2；销售订单保持 91、出库任务保持 77、待映射项保持 9。
- 54 个 SKU 汇总在两轮同步前后均为：总库存 3355、预留 405、可用 2950；没有库存流水写入。待映射 occurrence 总数按失败重试记录由 9 增至 15。
- 本轮新增消费者 ID 48、49，与失败原始事件和待映射记录一并保留；没有创建测试仓库、库位、批次或测试库存。

## 2026-07-25 最终验收

结论：**不通过**（历史结论，Shopify 缺陷已于 2026-07-26 关闭）。

整体不通过仅由以下两项组成：

1. Shopify retail mode 消费者事务边界问题已于 2026-07-26 修复并通过真实同步复验。
2. 采购 3/20/50 商品尚未在物理手机完成新建、编辑、保存操作。

运单功能与审计展示已通过。Shopify 不回写库存、履约、运单或订单是已确认产品边界，不是验收缺陷。

### Shopify retail mode

- 配置 1（`f6ce31-3.myshopify.com`）已切换为 `retailMode=true` 并保留。
- 真实同步结果：`success=0`、`skipped=0`、`failed=2`。
- 两张订单均先尝试建立轻量 `CONSUMER`，随后因未知 SKU 映射触发整单事务回滚；最终 `CONSUMER=0`、销售订单数仍为 91。
- 失败原始事件和待映射项保留；待映射总数为 9。没有库存预留、扣减或校正。
- 缺陷：消费者与订单共用事务，SKU 映射失败导致消费者一起回滚，未满足验收目标。按“不通过”规则暂停修复，等待用户确认方案。
- 当前只要求 Shopify→WMS 单向读取/导入，不要求 WMS→Shopify 回写；系统不得申请或调用库存、履约、运单及订单写接口。

### 运单

- 430px 销售订单详情显示运单号、承运商、状态、登记人、登记时间、作废人、作废时间和备注。
- 运单 2 为 `VOIDED`；API 审计字段为 `voidedBy=2`、`voidedByName=admin`、`voidedAt=2026-07-24T17:35:13.350938`。
- 运单审计展示定向测试 1 项及 TypeScript 类型检查通过；运单 2 继续保留。

### 采购多商品

- 采购单 56/57/58 分别含 3、20、50 个商品，430px 真实详情均能打开、滚动并核对总数量 3/20/50 与总额 £30/£200/£500。
- 大量商品详情可纵向滚动，宽表可横向滚动；未发现控件遮挡或页面崩溃。
- 三张单据保持 `ORDERING`，未点击确认 ASN、未收货，库存变化为 0，继续保留。

## 2026-07-25 修复后定向复验

> 历史快照；当前状态以本文顶部“2026-07-25 最终验收”及 2026-07-26 更新为准。

结论：**有条件通过**。

- 运单作废现记录 `voidedBy`、`voidedByName`、`voidedAt`；既有 VOIDED 运单 2 已回填操作人 2、名称 `admin` 和作废时间。
- 运单服务定向测试覆盖作废操作人及时间，共同定向回归 14 项通过。
- Shopify 实店配置仍为 `retailMode=false`，未获准切换或导入未知客户/订单，本轮没有触发同步，`CONSUMER` 建立项保留为外部条件。
- 采购 UAT 单 56/57/58 继续保留为 `ORDERING`；未确认、未收货、库存变化为 0。

验收日期：2026-07-25
验收人：Codex

## 初次验收总体结论（历史）

`不通过`

## Shopify retail_mode 只读同步

- 初次验收时启用配置指向 `f6ce31-3.myshopify.com`，当时 `retailMode=false`；后续已切换为 `true` 并执行同步。
- 当前有 42 个客户，`CONSUMER=0`、渠道来源客户 0；经营分析中的 `CONSUMER + CHANNEL` 为空。
- 本轮未改变店铺配置、未触发同步、未新增客户或订单。
- 阻塞：切换真实店铺的 retail mode 并同步会在本地新增客户和订单，执行前无法准确限定数量。需要用户明确确认该店铺可作为 UAT 数据源，以及同步结果保留或清理方式。

## 运单新增、修改、作废

- 使用测试销售单 `SO20260610001`（ID 13）。
- 新增测试运单后修改追踪号、承运商、链接、发货时间和备注，再成功作废。
- 测试运单 ID 2，最终状态 `VOIDED`，记录保留。
- 销售单状态、履约状态和金额前后不变，没有库存变化。
- 初次验收缺陷已于 2026-07-26 关闭：后端保存作废人和作废时间，页面现已展示登记人、登记时间、作废人和作废时间。

## 采购单多商品

- 定向前端测试 1 个采购表单文件通过；与集成相关的 2 个文件一并执行，共 3 个文件、12 项通过。
- 新建并核对三张 `ORDERING` UAT 采购单：
  - ID 56：3 个 SKU，总数量 3，总成本 30；
  - ID 57：20 个 SKU，总数量 20，总成本 200；
  - ID 58：50 个 SKU，总数量 50，总成本 500。
- 三张采购单仅停留在 `ORDERING`，没有确认、收货或库存写入。
- 三张单据使用 `P2-PURCHASE-UAT-20260725-*-RETAIN` 备注并明确保留；系统没有安全删除入口，不执行清理。
- 阻塞：应用内浏览器无法访问本机页面，少量、中量和大量商品的真机搜索、分页、键盘录入、横向滚动和汇总可见性未能真实复验。

依据“不通过”处理规则，暂停补充闭环后续验收；缺陷清单确认前不修复。

## 2026-08-09 历史测试数据归档后补充验收

结论：**有条件通过**。

- 订单 13/14及任务 1/2/3已从正常待发货/待拣队列排除，仍可通过作废状态追溯。
- 库存健康为 `UP`，归档前后批次、预留和库存流水无变化。
- 前端 44 个测试文件、143 项测试通过；应用内浏览器安全策略禁止控制局域网地址，本轮未宣称真实页面截图通过。
- 阻塞项：销售概览事实表为 15 张作废订单，而实时订单为 17张；事实刷新时间早于归档。必须修复归档后的报表事实失效/刷新机制，不能用人工刷新掩盖一致性缺陷。
- 详细证据见 `docs/HISTORICAL_TEST_DATA_POST_ARCHIVE_ACCEPTANCE_2026-08-09.md`。

### 2026-08-09 报表阻塞项关闭

- 已实现归档提交后的单日销售事实定向刷新，回滚时不执行，且不在归档事务内全量刷新。
- V4.48 修复上线前历史派生事实后，实时作废订单与销售概览均为 17，原 17/15 差异已关闭。
- 后端全量 125 套件/1287 项通过；本项由“不通过”更新为“通过”。局域网页面自动控制限制仍作为非业务阻塞项保留。
