# 前端分步验收待讨论事项

更新时间：2026-07-24

## 分步交付状态

- 第 1 步（骨架与认证闭环）：验收通过。
- 第 2 步（商品与库存查询）：功能交付完成；相关体验问题已列入后续统一评审。
- 第 3 步（销售、采购、入库业务流）：2026-07-13 有条件验收通过；`UI-REVIEW-003` 至 `UI-REVIEW-007` 不阻塞第 4 步，全部页面交付后统一处理。
- 第 4 步（渠道集成运营台）：功能与联调已完成；用户要求后续对四个入口及完整业务链路重新梳理，已登记为 `UI-REVIEW-009`，不阻塞第 5 步。真实渠道数据仅完成只读检查和连接测试，未执行同步、映射、启停或删除操作。
- 第 5 步（工作台）：功能与真实只读联调已完成；用户确认整体布局和核心经营数据展示需要重新设计，已登记为 `UI-REVIEW-010`，不阻塞本轮功能交付收尾。浏览器自动化工具当前无法连接本机页面，未执行真实审批操作。
- P2-A 第 1 模块（出库作业）：功能实现与自动化回归已完成，当前状态为“待用户验收”。已提供待拣货/拣货中/已完成队列、任务详情、单任务确认、批量事务确认，以及扫码批次与无码库位识别两种确认模式；浏览器自动化内核当前无法启动，因此保留人工视觉验收项，不将其登记为已验收。
- P2-A 第 2 模块（盘点作业与紧急库存校正）：功能实现、API 契约测试、权限边界测试和真实后端只读联调已完成，当前状态为“待用户验收”。盘点支持建单、启动、盲盘录入、差异复核和批准/退回；盲盘员无法读取账面快照，复核接口仅对复核角色开放。紧急校正支持已有批次与未入账批次两种建单方式，并将提交、复核、批准和实际落账拆分为独立动作；仓库管理员负责制单和落账，总经理负责复核与批准。前端全量 24 个测试文件、68 项测试通过，生产构建通过；只读联调验证了 14 条盘点中任务和 1 条已批准校正记录，未执行任何库存写操作。浏览器自动化内核仍无法启动，因此保留人工视觉验收项。
- P2 销售经营分析与客户采购事实层：后端汇总表、夜间刷新、查询接口、前端“经营分析”菜单、销售概览和客户采购分析均已完成，当前状态为“待用户验收”。页面只读汇总事实，不扫描业务明细；利润与 Localization 继续暂缓。后端 93 个测试套件、1110 项测试通过，前端 30 个测试文件、98 项测试通过并完成生产构建；真实数据库和浏览器桌面/窄屏均已核查。人工验收见 `docs/frontend/P2_SALES_ANALYTICS_ACCEPTANCE_CHECKLIST.md`。
- P2-B 仓库移动作业 PWA：收货、拣货和盲盘三个移动页面已完成，当前状态为“待用户验收”。收货要求逐项核验商品/批次和目标库位；拣货沿用后端路径顺序、库存预留与双批次追踪模式；盘点读取盲盘接口且不展示账面数量。PWA 仅缓存应用壳，`/api` 请求不进入缓存，断网时全部库存写操作停用。`V4_28` 已补齐 `WAREHOUSE_ADMIN` 与受限 `WAREHOUSE_STAFF` 的移动菜单及 7 项作业接口权限，不授予审批、复核或主数据维护能力。前端 32 个测试文件、105 项测试通过并完成生产构建；后端入库/出库/盘点定向回归 35 项通过；真实 430px 浏览器联调验证了 22 个待拣任务、1 张待收货单和 1 个进行中盘点任务，未执行库存写操作。人工验收见 `docs/frontend/P2_B_WAREHOUSE_PWA_ACCEPTANCE_CHECKLIST.md`。

### P2-A 后续任务：仓库与库位管理

- 状态：功能实现、后端契约扩展、自动化回归和真实后端只读联调均已完成，当前为“待用户验收”。
- 仓库管理：支持查询、搜索、启停、新建、编辑联系人与电话，并可直接进入对应仓库的库位页面；仓库编码创建后不可修改。
- 库位管理：支持按仓库、库区、启停状态和占用状态筛选；可维护二维路径坐标与备注。仓库、库区、货架号和位号共同组成库位身份，创建后不可修改；占用状态完全由库存业务自动维护。
- 防御规则：明确停用的仓库不得新增库位；停用已占用库位时前端显示库存清理风险，但保留通过正常出库或盘点清空现有库存的能力。
- 权限边界：超级管理员和仓库管理员可维护，仓库员工与总经理只读；更细粒度 IAM 权限将在完整功能交付后统一配置。
- 验证结果：后端 89 个测试套件、1092 项测试通过；前端 26 个测试文件、79 项测试通过，生产构建通过。只读联调核对 55 个仓库和 74 个库位，仓库库位汇总数逐仓一致，坐标字段无缺失。
- 人工验收：详见 `docs/frontend/P2_A_ACCEPTANCE_CHECKLIST.md`。

### P2 后续任务：身份与权限管理（IAM）

- 状态：功能实现、后端安全加固、OpenAPI 契约、自动化回归、真实只读联调和浏览器页面核查均已完成，当前为“待用户验收”。
- 用户账号：支持查询筛选、新建、资料编辑、启停、整组角色替换、默认角色选择、临时密码重置和逻辑删除；角色替换与默认角色在同一事务内提交。
- 角色权限：新增角色目录、权限目录和角色直接权限查询；前端提供只读审计页及权限明细抽屉，不开放角色定义与权限绑定写操作。
- 安全边界：禁止删除或停用自己、禁止修改自己的角色、禁止删除或移除最后一个启用的 `SUPER_ADMIN`；禁用角色不能分配；逻辑删除保留历史审计关系并撤销角色与访问资格。
- 验证结果：后端 91 个测试套件、1105 项测试通过；前端 29 个测试文件、94 项测试通过，生产构建成功。真实只读联调核对 1 个启用用户、4 个启用角色和 19 项权限，匿名访问角色目录返回 `403`。
- 浏览器核查：身份与权限手风琴菜单、用户账号列表、自我保护禁用态、角色审计列表和超级管理员 19 项直接权限抽屉均正常；未执行真实账号写操作。
- 人工验收：详见 `docs/frontend/IAM_ACCEPTANCE_CHECKLIST.md`。

## UI-REVIEW-001：商品管理与库存查询的功能边界

- 状态：暂缓讨论，待全部前端页面完成后统一评审。
- 用户反馈：当前“商品管理”和“库存查询”的功能划分、信息组织及实际使用方式存在疑问。
- 当前实现：商品管理负责商品主数据及新建/编辑；库存查询负责库存汇总、批次明细和库位反查。
- 后续评审重点：是否需要合并部分信息、增加跨页跳转、调整角色入口，或者按“商品档案”和“仓储作业”重新组织页面。

## UI-REVIEW-002：商品启用/停用语义需要闭环

- 状态：后端业务拦截已完成；权限和治理流程仍待设计。
- 权限决策：商品停用必须使用独立权限，至少与商品新建、编辑权限分离；前端按权限显示操作入口，后端必须独立鉴权，不能仅依靠前端隐藏按钮。
- 设计语义：`enabled=false` 应保留商品及历史记录，但禁止该商品进入新的入库、销售、预留和出库流程。
- 2026-07-24 已实现：新增统一 `ProductSkuOperationalPolicy`；产品（SPU）或 SKU 任一停用时，禁止创建新的销售单、采购单、入库计划和库存分配；启用 SKU 列表同时过滤停用的父产品。
- 在途规则：停用不回滚已经存在的收货或出库任务，既有单据允许按原流程完成，避免库存和财务链路中途断裂。
- 仍待决策：独立停用权限、停用申请与确认流程、停用原因和审计记录、重新启用权限、既有库存处置规则。

## UI-REVIEW-003：销售订单 Excel 模板改为可维护的静态模板

- 状态：2026-07-24 已完成真实下载与结构验收；模板视觉样式仍可由用户后续调整。
- 已实现：采用“外部静态模板优先、系统内置模板兜底、下载时刷新启用 SKU 参考表”的混合模式；外部文件缺失、损坏或结构错误时自动回退。
- 业务口径：客户在销售建单界面统一选择，Excel 只导入明细；删除了原模板中“要求填写但实际从未使用”的 `customerId` 列。
- 用户维护方式：运营人员可直接使用 Excel 修改外部模板的配色、字体、Logo、列宽和填写说明，不需要修改 Java 代码。
- 安全约束：下载和导入均校验工作表、表头、列顺序及数据起始行；改错结构会在建单前返回 `INVALID_EXCEL_DATA`。
- 验证结果：从真实销售页面下载 `sales_order_import_template.xlsx` 成功；工作簿包含 `Sales Order Lines`、`Instructions`、`SKU Reference`，机器表头正确，并实时写入 54 条启用 SKU 参考数据。
- 部署与字段说明：见 `docs/EXCEL_IMPORT_TEMPLATES.md`。

## UI-REVIEW-004：销售订单状态模型与渠道字段重新评审

- 状态：用户确认存在逻辑疑问，当前不处理，待全部页面交付后统一修改。
- 评审范围：渠道、订单状态、商业状态和履约状态的职责边界、命名、展示层级及状态流转关系。
- 当前疑问：`PARTIALLY_ALLOCATED`、`WAITING_INBOUND`、`BACKORDERED` 等状态同时出现在同一展示体系时容易产生重复或层级混淆；渠道字段的业务含义、来源和页面展示也需要重新确认。
- 后续目标：订单列表只展示用户完成日常工作所需的主状态；仓储执行状态、欠货数量和渠道来源在合适层级显示，避免把订单决策、库存分配和物理发货混成一条状态线。
- 约束：本阶段不修改后端状态机、接口字段或前端展示，先保持现有功能可用。

## UI-REVIEW-005：采购 Excel 模板方案待确认

- 状态：2026-07-24 已完成导入模板闭环，正式采购单打印继续作为独立后续能力。
- 已实现：采购页新增“下载导入模板”；采购模板复用外部优先、内置兜底及动态 SKU 参考表机制。
- 契约纠错：采购导入弹窗改为选择供应商主数据，上传 `supplierId`；修复了此前前端发送 `supplier` 名称、后端要求 `supplierId` 而必然返回 400 的问题。
- 业务口径：供应商在导入窗口统一选择，Excel 只导入采购明细；导入后创建 `ORDERING` 采购单，不绕过 ASN 与收货流程。
- 验证结果：从真实采购页面下载 `purchase_order_import_template.xlsx` 成功；工作簿包含 `Purchase Order Lines`、`Instructions`、`SKU Reference`，机器表头正确，并实时写入 54 条启用 SKU 参考数据。采购上传的写入链路仅在独立测试库验证，未向当前业务库制造测试采购单。
- 待后续：正式采购单 PDF/Excel 打印模板需单独定义公司抬头、审批签章、税务字段和版式，不与批量导入文件混用。

## UI-REVIEW-006：手动新建采购单与添加商品功能需完善

- 状态：2026-07-24 已完成首轮修复，待用户真机验收。
- 用户反馈：手动新建采购单中的“添加商品”功能存在问题，现有操作无法满足稳定、顺畅的建单需求。
- 已实现：仅加载启用产品下的启用 SKU；支持连续添加和删除明细行；禁止重复 SKU；数量、采购价、行小计、总数量与预计总额即时计算；错误定位到具体行；供应商/SKU 加载失败可重试；提交失败不清空表单。
- 待真机确认：实际数据下的商品搜索体验、单位显示和键盘录入效率。
- 验收目标：从选择供应商、连续添加多种商品到提交采购单形成完整闭环，错误必须明确定位到具体商品行。

## UI-REVIEW-007：采购管理多商品场景的页面扩展性

- 状态：2026-07-24 已完成大批量增强，待用户使用真实商品数据验收。
- 用户反馈：采购商品种类增加后，当前页面信息堆叠、操作区域混乱，难以浏览和修改。
- 已实现：订单头与采购明细分区、固定列宽的紧凑明细表、横向滚动保护、实时汇总、商品搜索和 Excel 导入入口；采购明细每页 20 行，支持选择当前页、跨页保留选择、批量设置单价/日期/备注和批量删除。
- 完整性保护：分页只改变当前编辑视图，订单总数量和预计金额始终按全部明细汇总；提交时校验全部页面，并在发现缺少 SKU、数量非法或 SKU 重复时自动跳转到问题所在页。
- 验收场景：至少使用少量、中等数量和大量商品三组数据验证布局，确保商品行增多时页面不无限拉长、关键操作不丢失、金额汇总始终可见。

## UI-REVIEW-008：渠道店铺增加自定义名称

- 状态：确认需要修改，当前不处理，待全部页面交付后统一实施。
- 用户反馈：店铺配置目前主要显示店铺域名，多店铺运行后不便于快速辨认和日常观察。
- 目标方案：每个渠道配置增加可编辑的“店铺名称/内部备注名”，例如“英国主店”“测试店”“欧洲批发店”；列表以自定义名称为主要识别信息，同时保留平台和店铺域名作为技术信息。
- 数据要求：名称应由后端持久化并在配置查询接口返回，不能只保存在浏览器本地；同一公司内建议校验名称非空且不重复，历史配置迁移时可暂用域名生成默认名称。
- 后续验收：新建和编辑配置均可填写名称，列表、同步提示、异常事件和 SKU 映射页面统一显示该名称，并能通过名称或域名搜索。

## UI-REVIEW-009：渠道集成业务链路与信息架构整体重梳理

- 状态：确认需要整体重新评审，当前保留已交付功能，待全部页面完成后逐步梳理和修改。
- 评审范围：店铺配置、待处理 SKU、SKU 映射管理、人工复核四个入口的名称、职责边界、使用顺序、角色权限、页面信息层级和跨页面跳转关系。
- 核心问题：当前页面按后端技术对象划分，业务人员不容易理解“现在发生了什么、为什么需要处理、处理后订单去哪里”，正常同步链路与异常处理链路也需要更清楚地区分。
- 梳理方法：先基于真实运营场景确定角色与任务，再绘制订单同步、商品识别、人工放行和失败恢复流程，最后确定页面结构、状态文案、操作权限及所需后端接口，不直接从现有菜单名称反推业务。
- 关联事项：`UI-REVIEW-008` 的店铺自定义名称纳入本专项统一设计；同时复核同步按钮、映射动作、虚拟商品、忽略策略和人工复核入口的业务风险提示。
- 验收目标：非技术运营人员进入渠道模块后，能够立即识别店铺、看到待办、理解处理后果，并能沿一条明确路径完成一次异常订单恢复。

## UI-REVIEW-010：工作台布局与经营分析体系重构

- 状态：第一阶段已完成。工作台继续承载待办，独立“经营分析”入口已交付销售概览与客户采购分析；利润、角色化首页、商品/渠道贡献仍待后续数据口径和第二轮体验设计。
- 用户反馈：现有工作台布局较混乱，无法直观看到利润、销售表现等核心经营数据，页面的信息优先级与管理视角不足。
- 推荐结构：采用“双层首页”。“工作台”面向日常执行，集中展示待审批、待映射、异常事件、缺货和快捷操作；已独立建立“经营分析”，首版承载销售额、订单量、客单价、趋势、流程状态和客户采购事实。毛利润、毛利率及商品/渠道贡献待数据口径齐备后扩展。
- 首页边界：工作台顶部可保留少量摘要指标和“进入经营分析”入口，但不堆叠完整图表；仓库、销售、采购和管理角色应看到各自优先级不同的首页内容。
- 数据前提：利润不能直接用销售额减商品主数据价格粗算，需要先确定销售收入、销售成本、退货、折扣、税费、运费、汇率和时间口径，并确认采用移动平均、批次实际成本或其他成本法。
- 实施顺序：先确认经营指标口径和角色需求，再设计汇总 API/汇总表，最后制作页面原型并开发，避免先画图表后补数据定义。
- 验收目标：用户进入系统后能够在十秒内回答“今天有什么必须处理、销售表现如何、利润是否异常、问题集中在哪里”。

## ARCH-REVIEW-011：多仓 SKU 经营策略下沉

- 状态：已确认加入待完成工作，当前仅登记方案，不修改数据库、接口或现有业务逻辑；计划纳入多仓能力建设阶段。
- 当前基线：`ProductSku` 目前以全局字段保存 `minStock`、`safetyStock` 和 `leadTime`。同一 SKU 在不同仓库只能共用一套阈值，无法表达广州仓安全库存 100、深圳仓安全库存 20 这类真实经营差异。
- 目标模型：新增“公司 + SKU + 仓库”唯一的经营策略关系，建议暂命名为 `sku_warehouse_policy`；商品主数据继续回答“它是什么”，关联策略回答“该商品在这个仓库如何经营”，库存批次与预留账本继续回答“现在有多少、已占用多少”。
- 策略范围：最低库存、安全库存、补货点、采购提前期、默认库区或默认上架范围、是否允许在该仓销售、是否允许采购进入该仓，以及结构化的仓库级补货/分配策略。不得使用无法校验的自由 JSON 代替明确字段和枚举。
- 口径前置：实施前必须先消除当前 `minStock` 与 `safetyStock` 语义重叠。建议将安全库存定义为需求波动缓冲量，将补货点定义为“提前期预计需求 + 安全库存”；“最低库存”若没有独立业务含义，应作为兼容字段迁移后废弃，不能保留三个含义近似的阈值。
- 业务接入：库存预警与补货建议按仓库独立计算；销售/ATP/预留必须遵守该仓销售开关；采购建单和收货目标仓必须遵守采购开关；默认库区只用于上架推荐，不能绕过库位防混放、批次隔离和容量校验。
- 数据迁移：根据现有 SKU 全局阈值，为当前已经营该 SKU 的仓库生成初始策略记录；迁移期间保留全局字段作为只读默认值，完成服务切换和数据核对后再废弃，禁止一次性删除导致旧流程失效。
- 前端范围：在 SKU 详情中增加“仓库策略”区域，以紧凑表格维护各仓阈值、经营开关和默认库区；不把这些仓库差异属性继续堆入商品基础信息表单。
- 架构约束：不得改变 `Available = OnHand - Reserved`、批次库存真相源、FEFO 和现有预占账本；仓库策略只决定可经营范围、预警和分配规则，不直接保存或修改库存数量。
- 验收场景：同一 SKU 在两个仓库可设置不同阈值并分别触发预警；关闭某仓销售或采购后只影响该仓；策略关系在同一公司内不得重复；历史订单和库存流水不因策略修改而被回写；无仓库策略时必须采用明确且可审计的默认行为。

## ARCH-REVIEW-012：Localization（语言、时区与业务日期口径）

- 状态：已确认加入 P2，当前登记设计边界，待仓储作业前端基础模块补齐后实施。
- 当前问题：JVM 默认时区已统一为 UTC，但 `ReportSummaryScheduler` 仍硬编码 `Asia/Shanghai`，部分预测逻辑又使用 `Europe/London`。如果继续各自计算“今天”，跨国运营时会出现订单归属日、日报日切和调度时间不一致。
- 数据原则：数据库时间戳统一保存 UTC；业务日期、日报归属日和门店日切由公司业务时区解释；界面显示时间可按用户偏好转换。不得把浏览器本地时间直接写成业务事实。
- 配置模型：公司级保存默认语言与业务时区，用户级保存显示语言与显示时区；采用 IANA 时区标识，不保存固定 `UTC+8` 这类无法正确处理夏令时的偏移量。
- 解析优先级：用户显示偏好 → 公司默认 → 系统默认。涉及结算、报表、库存日结和调度的业务时间必须使用公司业务时区，不得被个人显示偏好改变。
- 前端范围：顶部语言入口扩展为 Localization 设置，提供语言和时区选择，并明确区分“仅改变我的显示”与“修改公司业务时区”；后者只允许独立的系统管理权限操作并记录审计。
- 后端范围：集中提供时区解析与业务日期服务，逐步替换现有硬编码；先迁移报表刷新和预测日期，再覆盖渠道同步、审计展示及后续结算任务。
- 验收场景：同一 UTC 时刻在上海与伦敦用户界面显示不同本地时间，但同一公司的日报归属保持一致；伦敦夏令时切换日不重复或遗漏业务记录；修改个人语言/时区不改变库存与财务事实。

## ARCH-REVIEW-013：主数据治理中的导入模板管理

- 状态：用户已确认采用“主数据管理 / 数据治理 → 导入模板管理”的归属方案，但明确延期实施；当前继续使用服务器外部模板优先、内置模板兜底的机制。
- 业务定位：导入模板属于低频数据治理配置，不属于客户、商品或 SKU 主数据本体；前端可归入未来主数据管理系统，后端必须作为独立配置与文件版本能力管理，不得写入商品或客户业务表。
- 权限边界：普通业务人员仅可下载正式模板；模板查看、草稿上传、发布和回滚应拆分为独立权限。上传者不默认拥有发布权，所有发布与回滚动作必须记录操作者、时间、版本和校验结果。
- 目标流程：上传草稿 → 自动校验工作表与机器字段 → 预览 → 发布正式版本 → 保留历史版本 → 必要时回滚。发布失败不得影响当前正式模板，运行期仍须保留内置模板兜底。
- 实施时机：完成 P2 现有模块验收和业务流程梳理后再设计，不阻塞当前销售、采购 Excel 导入闭环。
