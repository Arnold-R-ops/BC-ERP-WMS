# IAM 管理端验收任务

## 2026-08-09 当前收尾状态

结论：**当前确认范围通过**。权限申请真实 API、审批隔离、授予/撤销、JWT 失效和不可变审计证据继续有效；最终只读收尾复验为后端 125 个套件/1287 项、前端 44 个文件/143 项全部通过，实时 OpenAPI、仓库快照和 TypeScript 类型一致，生产构建通过。下文较早的测试数量和“有条件通过”结论保留为当时历史证据，不代表当前状态。完整分类见 `../P2_FINAL_CLOSURE_AUDIT_2026-08-09.md`。

## 2026-08-08 权限申请端到端验收

结论：**通过（迁移、后端、前端、真实授权/撤销与真实 API）**。

- `V4_43__Permission_Request_Workflow.sql` 已由 Flyway 成功应用；建立申请、申请仓库、不可变审计 3 张表，待审目标—角色部分唯一索引、状态约束、乐观版本和审计防修改触发器均已落地。数据库实测审计 `UPDATE` 被拒绝并回滚。
- 已实现 `GET/POST /api/permission-requests`、`POST /api/permission-requests/{id}/review`、`POST /api/permission-requests/{id}/revoke`、`GET /api/permission-requests/{id}/audit`。实时 OpenAPI 和 `frontend/src/api/schema.d.ts` 已同步。
- 页面改为真实 API 分页、筛选、创建、批准、驳回和立即撤销，不再保存刷新即丢失的预览状态。页面明确说明批准/撤销会真实改变角色并使目标用户旧 JWT 失效。
- 仅允许申请启用、可分配的普通业务模板或 `CUSTOM + APPROVED + ACTIVE` 包；服务端独立阻止员工自助、管理员自授予、受保护角色、`SECURITY_ADMIN` 操作受保护身份、已分配角色和重复待审申请。
- 批准在同一事务中锁定申请、用户和角色，重算有效权限指纹，追加请求角色并保留既有/默认角色；`WAREHOUSE_STAFF` 的仓库分配与角色一起校验和提交。撤销只移除本申请角色，必须保留至少一个角色。
- 普通风险允许同一有权管理员复核；高风险同一申请人批准返回 403，真实第二 `SUPER_ADMIN` 批准成功。驳回和撤销意见必填，终态重复复核返回 409。
- 角色批准和撤销均推进目标用户安全版本、清理权限缓存；真实验收中两次旧 JWT 均在下一请求被拒。
- `CREATE/APPROVE/REVOKE` 与 `CREATE/REJECT` 审计链完整；目标用户逻辑删除后审计仍可读取。审计表没有依赖用户/角色生命周期的外键。
- 真实 API 验收 30 项通过，临时目标账号和第二审批人账号均已逻辑删除，未输出令牌/临时密码，未写入库存、预留、采购、销售或仓库作业数据。
- Java 全量 1262 项通过，失败/错误/跳过均为 0；前端 43 个测试文件、141 项通过，TypeScript 检查和生产构建通过；`/health/check` 返回 200，`git diff --check` 无空白错误。

## 2026-08-07 仓库员工页面与仓库数据范围验收

结论：**通过（自动化、迁移、健康检查、`SUPER_ADMIN` 用户管理页面及临时 `WAREHOUSE_STAFF` 真实 API 验收）**。

- 用户包含 `WAREHOUSE_STAFF` 时，创建或替换角色必须同时选择至少一个启用仓库，支持多选；移除该角色会清空仓库分配。`GET /api/users/assignable-warehouses` 只返回启用仓库。
- 数据范围仅在当前激活角色为 `WAREHOUSE_STAFF` 时生效。入库单、出库任务、盘点任务的列表、详情和作业写操作由后端校验；跨多个目标仓库的入库单必须全部属于员工分配仓库。
- 越权详情或伪造写操作返回明确的 403。无仓库分配、无有效仓库标识的历史异常数据均失败关闭；`WAREHOUSE_ADMIN` 保持不限仓。
- `WAREHOUSE_STAFF` 前端只显示工作台和移动收货、移动拣货、移动盲盘。直接访问其他页面会提示无页面权限并返回工作台；工作台不会启动销售、集成或库存建议等无权查询。
- 现有 8 项权限没有增加或改变。出库任务响应增加 `warehouseId`、`productSkuCode`，移动拣货使用任务自带的 SKU、批次跟踪模式和条码，不再调用 SKU 目录，也不授予 `product-sku:view`。
- `V4_42__Warehouse_Staff_Data_Scope.sql` 已由 Flyway 成功应用；后端 `/health/check` 返回 HTTP 200，实时 OpenAPI 和 TypeScript 类型已同步。
- 后端相关定向回归 106 项通过；Java 全量 1192 项通过。前端全量 40 个文件、133 项通过；TypeScript 检查和生产构建通过。
- 本次未创建测试用户，也未写入或修改库存、入库、出库、盘点等业务数据。历史 `WAREHOUSE_STAFF` 不自动回填仓库，管理员需在角色分配页明确补选。
- 2026-08-08 真实页面确认用户列表可展示作业仓库，历史员工显示未分配；修复了角色分配弹窗销毁后首次打开不回填已有角色/默认角色且不显示作业仓库字段的问题。修复后弹窗正确显示 `WAREHOUSE_STAFF`、默认角色和必填作业仓库多选；已取消弹窗，未提交数据。
- 2026-08-08 创建临时 `WAREHOUSE_STAFF` 验收账号并仅分配启用仓库 48。登录当前角色正确；用户管理接口为 403；待收货、待拣、拣货中和计数中列表均为 200，非空结果的仓库均为 48。未执行收货、拣货、盘点或库存写入。账号逻辑删除返回 204，旧令牌立即变为 403，活动账号残留为 0。
- 应用内浏览器在员工登录输入阶段连续丢失控制会话，因此没有把员工视角真实页面截图计入验收证据；菜单、工作台、三个移动页面和直接越权路由由前端自动测试覆盖，此项仅保留为浏览器稳定后的非阻塞复核。

## 2026-08-03 空白岗位权限包与勾选授权验收

结论：**通过（自动化与真实 API 负向验收；真实页面人工验收受浏览器本地 URL 安全策略阻断）**。

- 新增 `POST /api/roles/drafts` 和“新建权限包”弹窗；填写编码、名称和业务用途后，可从零权限开始，用复选框逐项选择直接权限。
- 新包固定为 `CUSTOM + DRAFT + DISABLED`，不自动建立继承、不自动启用、不自动分配用户；后续继续使用已有送审、简易审批、显式启用和用户分配流程。
- 页面只展示 `ACTIVE + custom_assignable=true + 非 CRITICAL` 权限并支持搜索；`HIGH` 权限允许勾选但保留高风险送审和不同超级管理员复核规则。
- 受保护权限不会出现在可选列表，后端仍独立校验。真实 API 提交受保护权限返回 400，事务回滚，开发库中测试草稿为 0，`/health/check` HTTP 200。
- 空白创建相关后端定向测试 20 项通过；Java 全量 1183 项通过；前端 39 个测试文件、130 项通过；TypeScript 检查和生产构建通过。
- 实时 OpenAPI 和 `frontend/src/api/schema.d.ts` 已同步新端点与请求类型；成功创建路径由服务事务测试、控制器测试、API 契约测试和弹窗组件测试覆盖。
- 未向开发库创建无业务用途的权限包或不可变审计。2026-08-06 再次启动前后端服务并确认 HTTP 200；应用内浏览器控制已恢复，但本地私网 URL 被安全策略拦截且禁止换用其他自动化方式绕过，因此仍需用户在本机浏览器补一次人工页面点击验收。
- 2026-08-06 前端定向复验 2 个文件、8 项通过；后端本机当前未提供 Maven 可执行命令，未重复运行后端定向测试，沿用 2026-08-03 已通过的 20 项定向测试和 1183 项全量测试证据。
- 普通业务模板权限基线已按暂定方案完成；空白创建和逐项勾选仍是最简主流程，模板复制作为可选快捷方式保留。

## 2026-08-02 受保护安全管理员验收

结论：**通过（真实 API 写入仅使用临时账号，活动账号已清理）**。

- `SECURITY_ADMIN` 固定为 `SYSTEM + PRIVILEGED + import_allowed=false`，没有 `SUPER_ADMIN` 全局绕过，也不持有 `system:admin`。
- 可读取用户、角色和权限目录；可创建、更新和删除普通用户。真实 API 状态分别验证为 200、201/200/204。
- 访问全局集成管理端点、修改 `SUPER_ADMIN` 身份、给用户授予 `SECURITY_ADMIN` 均返回 403；被拒绝的用户创建未落库。
- 高风险权限包仍只允许另一名 `SUPER_ADMIN` 批准；`SECURITY_ADMIN` 可驳回但批准按钮禁用，后端也会拒绝批准。
- `V4_39` 建立受保护角色，`V4_40` 修复目录集合根路径，`V4_41` 为缺少历史 V2 权限种子的升级库幂等补齐并授权 IAM 目录权限；开发库当前为 V4.41，健康检查 HTTP 200。
- 安全管理员相关后端定向测试 56 项通过；Java 全量 1180 项通过；前端 38 个测试文件、128 项通过；TypeScript 检查和生产构建通过。
- 真实安全管理员验收 9/9 项通过；令牌和临时密码未落盘、未在文档中记录，所有活动临时账号已清理。

## 2026-08-02 自定义岗位权限包简易审批生命周期验收

结论：**通过（真实页面仅做只读验收，未生成测试权限包）**。

- 数据库已应用 `V4_37`、`V4_38`：内置受保护模板 `ROLE_PACKAGE_SIMPLE_APPROVAL`、角色审批状态和不可变治理审计已落地；`FORMAL` 仅预留，未实现通用审批引擎。
- 新复制包固定为 `DRAFT + DISABLED`；草稿可编辑名称、用途和直接权限，不允许在线修改继承；送审后锁定，驳回需填写意见并返回草稿。
- 简易审批为单级：普通风险允许同一超级管理员复核；高风险要求业务理由且必须由另一超级管理员账号批准。
- 批准后仍保持停用；启用/停用均要求理由和权限包编码确认。只有 `CUSTOM + APPROVED + ACTIVE` 可分配给用户，启用包变更必须复制新版本。
- 停用是紧急收权动作，不受历史权限后来停用/不可分配影响；权限变化和启停会推进安全版本并清理缓存。
- 后端相关定向测试 62 项通过；Java 全量 1174 项通过；前端 38 个测试文件、126 项通过；TypeScript 检查、生产构建和 `git diff --check` 通过。
- 实时 OpenAPI 和前端类型已同步；开发库迁移到 `V4_38`，健康检查 HTTP 200。
- 真实岗位权限包页面已显示审批状态筛选和列，系统角色显示“系统维护”，受保护角色无在线治理入口；当前库没有自定义包，因此未创建、送审、审批、启停或分配测试数据。页面控制台无应用错误。

## 2026-08-02 岗位权限包复制验收

结论：**通过**。

- 后端新增 `GET /api/roles/{id}/copy-preview` 和 `POST /api/roles/{id}/copies`，仅 `SUPER_ADMIN` 可调用；OpenAPI 快照和前端类型已同步。
- 普通业务模板 `GENERAL_MANAGER`、`SALESPERSON`、`WAREHOUSE_STAFF`、`WAREHOUSE_ADMIN` 显示复制入口；受保护的 `SUPER_ADMIN`、`SECURITY_ADMIN` 不显示入口。
- 真实页面中 `WAREHOUSE_STAFF` 预览为 8 项有效权限、5 项普通风险、3 项高风险、0 项受保护权限。
- 页面明确说明复制为独立直接权限，不复制继承关系、用户分配或临时授权，源模板后续变化不传播。
- 高风险分支显示业务理由、风险勾选和新权限包编码复核；`CRITICAL`、不可分配或系统保留权限由前后端共同阻断。
- 新包结果页明确显示 `CUSTOM + DISABLED`、权限数、高风险数和审计记录号；自动测试覆盖创建成功、过期指纹、重复编码和风险确认失败。
- Java 全量测试 1164 项通过；前端 37 个测试文件、121 项通过；TypeScript 检查和生产构建通过。
- 本轮真实页面验收未提交创建，未留下无业务用途的测试角色或复制审计记录。

## 2026-08-01 当前激活角色鉴权闭环验收

结论：**通过**。

- Maven 当前角色鉴权定向测试 121 项通过；最新 Java 全量测试 1164 项通过，均为失败 0、错误 0、跳过 0。
- 真实 IAM API 验收 25/25 项通过。同一临时账号同时分配 `WAREHOUSE_STAFF` 和 `SUPER_ADMIN` 时，默认激活低权限角色；仓库只读接口返回 200，IAM 目录返回 403，证明其他已分配角色没有串权。
- 切换为 `SUPER_ADMIN` 后新令牌可访问 IAM，旧低权限令牌立即被拒绝；撤销高权限角色后旧高权限令牌也立即被拒绝。
- 停用账号后旧令牌和新登录均返回 401/403；重新启用后可以重新登录，默认角色仍为 `WAREHOUSE_STAFF`。
- 密码重置后旧令牌及旧密码立即失效；临时密码登录返回 `mustChangePassword=true`，未代替用户执行最终改密步骤。
- 逻辑删除返回 204；删除后临时密码和删除前令牌均返回 401/403。
- 验收账号 `codex_iam_20260801_2110`、`codex_iam_20260801_2130` 均已逻辑删除，仅保留审计痕迹；没有修改系统角色或权限绑定，没有写入库存、预留、采购、销售、收货、拣货或盘点数据。
- 实时 OpenAPI 已同步到 `docs/frontend/openapi.json` 并重新生成 `frontend/src/api/schema.d.ts`。

## 2026-07-25 最终验收

结论：**通过**。

- 430×900 真实用户页面完成搜索、状态/角色筛选、新建、编辑、分配角色、重置密码和删除入口验收；角色权限页面以只读审计模式展示 5 个角色及查看权限入口。
- 临时用户 32 完成创建和登录；管理员重置后原令牌访问普通接口为 403。
- 临时密码登录返回 `mustChangePassword=true`，改密前普通接口 403；完成自助改密返回 200。
- 停用账号后新令牌立即返回 403；临时账号最终逻辑删除。
- `WAREHOUSE_STAFF` 直接访问 IAM 页面被路由守卫重定向，真实接口访问用户管理同样为 403。
- 没有修改现有用户、角色或权限目录。

## 2026-07-25 权限闭环定向复验

结论：**有条件通过**。

- JWT 认证现只为令牌 `current_role` 加载直接及继承权限码，不合并用户其他已分配角色，保持角色切换隔离。
- 临时 `WAREHOUSE_STAFF` 可访问三个移动作业读取接口但不可访问用户管理；临时 `GENERAL_MANAGER` 可读取经营分析但不可刷新报表或访问用户管理。
- 两个临时账号均已逻辑删除；此前停用、密码重置、强制改密和旧令牌失效的真实接口证据继续有效。
- 真实 IAM 管理页面交互仍需在能够访问本机前端的浏览器环境补验。

更新时间：2026-07-22

## 1. 本次交付范围

1. 用户账号：查询、新建、编辑、启停、角色替换、默认角色、密码重置和逻辑删除。
2. 角色与权限：角色目录、角色直接权限、继承关系和全局权限目录的只读审计。
3. 防锁死保护：禁止自我停用、自我改权、自我删除，以及删除或降权最后一个启用的超级管理员。
4. 原子性：角色集合与默认角色一次提交，任一步失败均不保留半成功状态。

角色定义、角色删除和权限绑定暂不属于本轮。历史权限路径规范化后再开放写操作。

## 2. 自动验证结果

| 验证项 | 结果 |
|---|---|
| 后端完整回归 | 91 个测试套件，1105 项通过，失败 0 |
| 前端完整回归 | 29 个测试文件，94 项通过，失败 0 |
| 前端生产构建 | 通过 |
| OpenAPI/TypeScript 契约 | 已从当前 8080 后端重新生成 |
| 真实数据只读联调 | 1 个启用用户、4 个启用角色、19 项权限 |
| 未认证访问角色目录 | `403 Forbidden` |
| 浏览器页面核查 | 菜单、用户页、自我保护、角色页和权限抽屉均正常 |

自动联调没有创建、停用、改权、重置或删除真实账号。

## 3. 验收准备

- 后端：`http://127.0.0.1:8080`
- 前端：`http://127.0.0.1:5173/#/system/users`
- 使用 `SUPER_ADMIN` 登录。
- 建议只操作本次新建的验收账号，用户名使用 `uat_iam_日期序号`，不要改动正式账号。

## 4. 任务 A：菜单与只读目录

- [ ] 左侧“身份与权限”可展开，包含“用户账号”和“角色与权限”。
- [ ] 用户账号页可按关键字、启停状态和角色筛选。
- [ ] 角色与权限页显示角色类型、状态、用户数、直接权限数和继承角色数。
- [ ] 打开 `SUPER_ADMIN` 权限抽屉，可看到权限编码、类型、受保护资源和状态。
- [ ] 页面明确标记为“只读审计”，没有新增角色、删除角色或修改权限按钮。

## 5. 任务 B：新建验收账号

- [ ] 点击“新建账号”，填写唯一用户名、显示名称和符合规则的初始密码。
- [ ] 至少选择一个启用角色，并选择默认角色。
- [ ] 保存后账号出现在列表中，角色和默认角色显示正确。
- [ ] 重复用户名被明确拒绝；禁用角色不能被选择或提交。

## 6. 任务 C：资料与角色

- [ ] 编辑验收账号的显示名称、备注和启停状态，刷新页面后保持一致。
- [ ] 打开“分配角色”，一次替换多个角色并更换默认角色。
- [ ] 保存后角色集合与默认角色同时正确；失败时不应出现只改成功一半的状态。
- [ ] 当前登录的 admin 账号不能停用自己，分配角色按钮也处于禁用状态。

## 7. 任务 D：密码与访问撤销

- [ ] 对验收账号执行密码重置，只显示一次临时密码。
- [ ] 使用临时密码登录后，系统要求先修改密码再访问其他业务页面。
- [ ] admin 停用该验收账号后，该账号的旧登录凭证不能继续访问 API。
- [ ] 重新启用后可按正常认证流程恢复使用。

## 8. 任务 E：逻辑删除收尾

- [ ] 删除本次验收账号，列表中不再显示该账号。
- [ ] 被删除账号的旧凭证立即失效。
- [ ] 历史业务记录不因账号删除而丢失或断链。
- [ ] admin 自己的删除按钮保持禁用，系统不会允许删除最后一个启用的 `SUPER_ADMIN`。

## 9. 验收结论

- 验收结果：`有条件通过`
- 阻塞问题：应用内浏览器无法访问本机管理页面，菜单、筛选、弹窗、一次性临时密码展示和页面禁用态未能在本轮真实复验。
- 非阻塞优化：当前真实目录为 5 个角色、27 项权限，原清单中的“4 个角色、19 项权限”基线需要更新。
- 验收人：Codex
- 验收日期：2026-07-25

### 已验证

- 临时账号成功完成新建、资料编辑、角色集合替换与恢复。
- 重复用户名返回 HTTP 409。
- admin 自我停用返回 HTTP 403。
- 密码重置后 `mustChangePassword=true`，业务接口返回 HTTP 403；修改密码成功后恢复正常登录。
- 停用临时账号后旧令牌返回 HTTP 403；重新启用后可重新登录。
- 逻辑删除返回 HTTP 204，账号从列表消失，删除前令牌返回 HTTP 403。
- 匿名访问角色目录返回 HTTP 403；超级管理员可读取 5 个角色和 27 项权限。

### 测试数据处置

- `uat_p2b_20260725_01` 已逻辑删除，不保留可登录账号。
- 没有修改 admin、系统角色或权限绑定，没有业务数据及库存变化。

条件项只允许补齐真实页面复验，不扩大为 IAM 重构；Localization 继续暂缓。
