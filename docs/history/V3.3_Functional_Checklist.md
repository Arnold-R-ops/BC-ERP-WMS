# BC ERP-WMS V3.3 功能验收清单 (Functional Checklist)

> 基于 DEVELOPMENT_GUIDE.md 核心业务流程 & API 接口文档
> 生成时间：2026-01-18
> 迁移状态：Phase 1 已完成（数据库 & 实体层）

---

## 目录
- [1. 产品管理模块（SPU-SKU 层级）](#1-产品管理模块spu-sku-层级)
- [2. 采购管理模块（三阶段流程）](#2-采购管理模块三阶段流程)
- [3. 库存管理模块（多库位批次）](#3-库存管理模块多库位批次)
- [4. 批次管理模块（Hashids + FIFO）](#4-批次管理模块hashids--fifo)
- [5. 库位管理模块](#5-库位管理模块)
- [6. 认证授权模块](#6-认证授权模块)
- [7. 数据完整性 & 约束](#7-数据完整性--约束)

---

## 1. 产品管理模块（SPU-SKU 层级）

### 🔑 V3.3 核心架构：SPU-SKU 层级管理

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **1.1 SPU 表创建** | ✅ 必须存在 `product_spu` 表，包含 `spu_code`（唯一）、`spu_name`、`category`、`brand` 等字段 | ✅ **已完成 (Phase 1)** | `SELECT * FROM information_schema.tables WHERE table_name = 'product_spu'` |
| **1.2 默认 SPU 初始化** | ✅ 必须有 ID=0 的默认 SPU（`DEFAULT-SPU`）用于旧数据兼容 | ✅ **已完成 (Phase 1)** | `SELECT * FROM product_spu WHERE id = 0 AND spu_code = 'DEFAULT-SPU'` |
| **1.3 Product-SPU 外键关联** | ✅ `products.spu_id` 必须外键关联到 `product_spu(id)`，约束名 `fk_product_spu` | ✅ **已完成 (Phase 1)** | `SELECT * FROM pg_constraint WHERE conname = 'fk_product_spu'` |
| **1.4 SKU 名称字段** | ✅ `products.sku_name` (VARCHAR 100, NOT NULL) 用于区分同 SPU 下的不同 SKU | ✅ **已完成 (Phase 1)** | `SELECT column_name FROM information_schema.columns WHERE table_name = 'products' AND column_name = 'sku_name'` |
| **1.5 规格描述字段** | ✅ `products.specs` (VARCHAR 500, NULL) 用于存储 SKU 规格（JSON 或文本） | ✅ **已完成 (Phase 1)** | `SELECT column_name FROM information_schema.columns WHERE table_name = 'products' AND column_name = 'specs'` |
| **1.6 创建 SPU（API）** | 🔵 POST `/api/product-spu` 创建新产品家族（需校验 `spu_code` 唯一性） | ⏳ **待实现 (Phase 2)** | Postman 调用 API，检查返回 201 和 `spu_id` |
| **1.7 查询 SPU 下所有 SKU** | 🔵 GET `/api/product-spu/{id}/skus` 返回某 SPU 下的所有 SKU 列表 | ⏳ **待实现 (Phase 2)** | Postman 调用，验证返回 SKU 列表 |
| **1.8 创建 SKU 并关联 SPU** | 🔵 POST `/api/products` 创建 SKU 时必须指定 `spu_id`（不能为 NULL） | ⏳ **待实现 (Phase 2)** | Postman 发送 `{"spuId": 1, "skuName": "绿茶-箱装", ...}`，检查 DB 中 `spu_id` 正确 |
| **1.9 SKU 数量格式化** | 🔵 Product 实体的 `formatQuantity(int qty)` 方法：将数量转为 "2 Box, 1 Unit" 格式 | ⏳ **待实现 (Phase 2)** | 单元测试：`product.formatQuantity(25)` → "2 Box, 5 Unit"（假设 conversionRate=12） |
| **1.10 零头判断逻辑** | 🔵 Product 实体的 `isLooseQuantity(int qty)` 方法：判断是否为零头（`qty % conversionRate != 0`） | ⏳ **待实现 (Phase 2)** | 单元测试：`product.isLooseQuantity(13)` → `true` |

---

## 2. 采购管理模块（三阶段流程）

### 🔑 V3.3 核心架构：ORDERING → IN_TRANSIT → COMPLETED

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **2.1 Stage 1: 创建采购单** | 🔵 POST `/api/purchase-orders` 创建采购单，自动生成 `po_number`（格式：PO-20250117-001），状态为 `ORDERING` | ⏳ **待实现 (Phase 2)** | Postman 调用，检查返回的 `poNumber` 格式和 `status = ORDERING` |
| **2.2 采购单号自增逻辑** | 🔵 同一天的采购单号序号自增（001 → 002 → 003），跨天重置 | ⏳ **待实现 (Phase 2)** | 创建多个采购单，验证序号递增 |
| **2.3 采购单明细关联** | 🔵 `purchase_order_item` 表必须关联 `purchase_order.id` 和 `products.id` | ⏳ **待实现 (Phase 2)** | 创建采购单后查询 `purchase_order_item` 表，验证 `purchase_order_id` 和 `product_id` |
| **2.4 Stage 2: 确认 ASN** | 🔵 POST `/api/purchase-orders/{id}/confirm-asn` 提供 `expiryDate`（必填），生成 Hashids 批次码，状态变更为 `IN_TRANSIT` | ⏳ **待实现 (Phase 2)** | Postman 调用，检查 `status = IN_TRANSIT` 且返回 `batchCode`（6位 Hashids） |
| **2.5 批次码生成规则** | ✅ **关键**：使用 Hashids 生成 6 位批次码（如 `R7M4K9`），salt 从环境变量 `BATCH_SALT` 读取 | ⏳ **待实现 (Phase 2)** | 配置 `BATCH_SALT` 环境变量，调用 API，验证批次码长度=6 且可重复解码 |
| **2.6 批次初始状态** | 🔵 确认 ASN 后，创建 `inventory_batch` 记录：`location_id = NULL`，`entry_date = NULL`，`active = true` | ⏳ **待实现 (Phase 2)** | 查询 `inventory_batch`，验证 `location_id IS NULL` 且 `entry_date IS NULL` |
| **2.7 Stage 3: 实物入库** | 🔵 POST `/api/purchase-orders/{id}/receive` 扫描批次码 + 分配库位，更新 `location_id` 和 `entry_date`，状态变更为 `COMPLETED` | ⏳ **待实现 (Phase 2)** | Postman 调用，检查批次的 `location_id` 已赋值且 `entry_date = CURRENT_TIMESTAMP` |
| **2.8 部分收货逻辑** | 🔵 若 `receivedQuantity < orderedQuantity`，状态为 `PARTIALLY_RECEIVED`；全部收货后变为 `COMPLETED` | ⏳ **待实现 (Phase 2)** | 创建100件采购单，只收货50件，验证 `status = PARTIALLY_RECEIVED` |
| **2.9 状态回退机制** | 🔵 支持 `IN_TRANSIT → ORDERING` 回退，作废所有已生成批次码（`active = false`），记录回退原因到 `audit_log` | ⏳ **待实现 (Phase 2)** | 调用回退 API，验证批次的 `active = false` 且 `audit_log` 包含原因 |
| **2.10 审计日志** | 🔵 所有状态变更必须记录到 `purchase_order.audit_log`（JSON 格式），包含时间戳、操作人、原因 | ⏳ **待实现 (Phase 2)** | 查询 `audit_log` 字段，验证包含 `{"timestamp": "...", "operator": "admin", "reason": "..."}` |

---

## 3. 库存管理模块（多库位批次）

### 🔑 V3.3 核心架构：InventoryBatch 单一数据源 + 多库位支持

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **3.1 location_code 字段** | ✅ **关键**：`inventory_batch.location_code` (VARCHAR 50, NOT NULL) 冗余字段，用于快速查询 | ✅ **已完成 (Phase 1)** | `SELECT column_name FROM information_schema.columns WHERE table_name = 'inventory_batch' AND column_name = 'location_code'` |
| **3.2 多库位批次索引** | ✅ 必须有复合索引 `idx_batch_location (batch_code, location_code)` 支持同批次在多库位查询 | ✅ **已完成 (Phase 1)** | `SELECT indexname FROM pg_indexes WHERE indexname = 'idx_batch_location'` |
| **3.3 batch_code 非唯一** | ✅ **关键变更**：batch_code 不再有唯一约束（允许同批次分散在多个库位） | ✅ **已完成 (Phase 1)** | `SELECT * FROM pg_constraint WHERE conrelid = 'inventory_batch'::regclass AND contype = 'u' AND pg_get_constraintdef(oid) LIKE '%batch_code%'` 应返回 0 行 |
| **3.4 库存查询聚合** | 🔵 查询商品总库存：`SUM(quantity) FROM inventory_batch WHERE product_id = ? AND active = true` | ⏳ **待实现 (Phase 2)** | 调用 `InventoryBatchRepository.sumTotalQuantityByProduct(productId)` |
| **3.5 库位库存查询** | 🔵 查询某库位的某商品库存：按 `product_id + location_id + active = true` 过滤后聚合 | ⏳ **待实现 (Phase 2)** | `findByProductAndLocationAndActiveTrue(product, location)` 返回批次列表 |
| **3.6 跨库位批次查询** | 🔵 查询同批次在不同库位的分布：`SELECT location_code, SUM(quantity) FROM inventory_batch WHERE batch_code = ? GROUP BY location_code` | ⏳ **待实现 (Phase 2)** | 手动执行 SQL，验证同批次可在多个库位 |
| **3.7 入库 API** | 🔵 POST `/api/inventory/adjust` 类型 `IN`，必须指定 `locationId`，同步更新 `location_code` | ⏳ **待实现 (Phase 2)** | Postman 调用，检查 `inventory_batch` 的 `location_code` 与 `locations.locationcode` 一致 |
| **3.8 出库 API** | 🔵 POST `/api/inventory/adjust` 类型 `OUT`，执行 FIFO 策略，扣减最早批次 | ⏳ **待实现 (Phase 2)** | 出库后验证最早批次的 `quantity` 减少 |
| **3.9 库存不足异常** | 🔵 出库数量 > 可用库存时，抛出 `STOCK_INSUFFICIENT` 异常（ErrorKey.STOCK_INSUFFICIENT） | ⏳ **待实现 (Phase 2)** | Postman 调用出库 API（超量），验证返回 400 且 errorKey = `STOCK_INSUFFICIENT` |
| **3.10 库存流水记录** | 🔵 每次入库/出库生成 `stock_transaction` 记录，包含 `quantity_before`、`quantity_after`、`operator` | ⏳ **待实现 (Phase 2)** | 查询 `stock_transaction` 表，验证有对应记录 |

---

## 4. 批次管理模块（Hashids + FIFO）

### 🔑 V3.3 核心架构：Hashids 批次码 + FIFO 零头优先

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **4.1 Hashids 配置** | ✅ `application.yml` 中 `batch.hashids.salt` 必须从环境变量 `BATCH_SALT` 读取（安全要求） | ⏳ **待验证 (配置)** | 检查 `application.yml`，确认 `salt: ${BATCH_SALT:...}` |
| **4.2 批次码长度** | ✅ 批次码固定 6 位（`min-length: 6`） | ⏳ **待验证 (配置)** | 检查 `application.yml`，确认 `min-length: 6` |
| **4.3 批次码生成服务** | 🔵 `BatchCodeService.generateBatchCode()` 使用 Hashids 编码 `purchaseOrderItemId`，可逆解码 | ⏳ **待实现 (Phase 2)** | 单元测试：生成批次码后调用 `decodeBatchCode()` 验证能还原原始 ID |
| **4.4 批次码唯一性** | 🔵 数据库索引 `idx_batch_code` 确保批次码不重复（虽然 batch_code 非唯一约束，但生成器保证唯一） | ✅ **已完成 (Phase 1)** | `SELECT indexname FROM pg_indexes WHERE indexname = 'idx_batch_code'` |
| **4.5 FIFO 出库策略** | 🔵 `InventoryService.adjustStock()` 出库时按 `entry_date ASC` 排序，优先扣减最早批次 | ⏳ **待实现 (Phase 2)** | 创建3个批次（不同 entry_date），出库后验证最早批次被扣减 |
| **4.6 零头优先策略** | 🔵 **关键逻辑**：FIFO 排序后，优先扣减零头批次（`quantity % conversionRate != 0`），不足时才拆新箱 | ⏳ **待实现 (Phase 2)** | 创建1个零头批次（5件）+1个整箱批次（12件），出库6件，验证零头先扣 |
| **4.7 过期批次预警** | 🔵 `InventoryBatchRepository.findExpiringBatches(date)` 查询即将过期的批次（`expiry_date <= date AND active = true`） | ⏳ **待实现 (Phase 2)** | 查询 7 天内过期的批次，验证返回正确 |
| **4.8 批次作废** | 🔵 `InventoryBatch.markAsInactive(reason)` 设置 `active = false`，记录作废原因到 `remark` | ⏳ **待实现 (Phase 2)** | 调用方法后验证 `active = false` 且 `remark` 有内容 |
| **4.9 乐观锁并发控制** | ✅ `inventory_batch.version` 字段（BIGINT）用于乐观锁，防止并发扣减冲突 | ⏳ **待验证 (实体)** | 检查 `InventoryBatch` 实体是否有 `@Version` 注解 |
| **4.10 批次查询 API** | 🔵 GET `/api/inventory-batches?batchCode={code}` 查询批次详情（包括库位、数量、过期日期） | ⏳ **待实现 (Phase 2)** | Postman 调用，验证返回批次信息 |

---

## 5. 库位管理模块

### 🔑 V3.3 核心要求：库位层级结构 + location_code 生成

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **5.1 库位表结构** | ✅ `locations` 表包含 `warehouse_code`、`zone`、`shelf_number`、`position_number`、`locationcode`（注意：全小写） | ✅ **已完成 (Phase 1)** | `SELECT column_name FROM information_schema.columns WHERE table_name = 'locations'` |
| **5.2 库位码格式** | ✅ `locationcode` 格式：`WH1-ZONE_A-01`（仓库-库区-货位号） | ✅ **已验证 (数据)** | `SELECT locationcode FROM locations LIMIT 5` 验证格式 |
| **5.3 库位枚举管理** | 🔵 `zone` 字段使用枚举（ZONE_A, ZONE_B, ZONE_C, ZONE_D） | ⏳ **待实现 (Phase 2)** | 检查 `Location` 实体是否使用 `@Enumerated` |
| **5.4 库位容量管理** | 🔵 `capacity` 字段限制库位最大存储量 | ⏳ **待实现 (Phase 2)** | 创建库位时指定 `capacity`，验证入库时不超容 |
| **5.5 库位启用/禁用** | 🔵 `enabled` 字段控制库位是否可用，禁用后不允许新增入库 | ⏳ **待实现 (Phase 2)** | 设置 `enabled = false`，尝试入库，验证返回错误 |
| **5.6 创建库位 API** | 🔵 POST `/api/locations` 创建库位，自动生成 `locationcode` | ⏳ **待实现 (Phase 2)** | Postman 调用，验证 `locationcode` 自动生成 |
| **5.7 查询库位库存** | 🔵 GET `/api/locations/{id}/inventory` 查询某库位的所有批次和商品库存 | ⏳ **待实现 (Phase 2)** | Postman 调用，验证返回批次列表 |

---

## 6. 认证授权模块

### 🔑 V3.3 核心要求：JWT 认证 + 角色权限

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **6.1 JWT 密钥配置** | ✅ `jwt.secret` 必须从环境变量 `JWT_SECRET` 读取（至少 256 位） | ⏳ **待验证 (配置)** | 检查 `application.yml`，确认 `secret: ${JWT_SECRET}` |
| **6.2 Token 有效期** | ✅ `jwt.expiration: 86400000` (24 小时) | ⏳ **待验证 (配置)** | 检查 `application.yml` |
| **6.3 登录 API** | 🔵 POST `/api/auth/login` 返回 JWT token，包含 `username`、`role`、`expiresIn` | ⏳ **待实现 (Phase 2)** | Postman 调用，验证返回 token 字符串 |
| **6.4 Token 验证** | 🔵 所有业务 API 需校验 `Authorization: Bearer <token>` 头部 | ⏳ **待实现 (Phase 2)** | 不带 token 调用业务 API，验证返回 401 |
| **6.5 角色权限控制** | 🔵 支持 `ADMIN`、`OPERATOR` 角色，不同角色有不同 API 访问权限 | ⏳ **待实现 (Phase 3)** | OPERATOR 角色调用管理员 API，验证返回 403 |

---

## 7. 数据完整性 & 约束

### 🔑 V3.3 核心要求：外键约束 + 唯一索引 + 审计字段

| 功能点 | V3.3 核心要求 | 当前状态 | 验证方法 |
|-------|-------------|---------|---------|
| **7.1 外键约束检查** | ✅ 必须存在 `fk_product_spu` 约束（products → product_spu） | ✅ **已完成 (Phase 1)** | `SELECT conname FROM pg_constraint WHERE conname = 'fk_product_spu'` |
| **7.2 唯一索引检查** | ✅ `product_spu.spu_code` 必须有唯一索引 `product_spu_spu_code_key` | ✅ **已完成 (Phase 1)** | `SELECT indexname FROM pg_indexes WHERE indexname LIKE '%spu_code%' AND indexdef LIKE '%UNIQUE%'` |
| **7.3 级联删除保护** | 🔵 删除 SPU 时，如果有关联 SKU，必须返回 `FOREIGN_KEY_VIOLATION` 错误 | ⏳ **待实现 (Phase 2)** | 尝试删除有 SKU 的 SPU，验证返回错误 |
| **7.4 审计字段自动填充** | 🔵 所有实体继承 `BaseEntity`，`created_at` 和 `updated_at` 自动填充 | ⏳ **待实现 (Phase 2)** | 创建记录后验证 `created_at` 有值 |
| **7.5 NOT NULL 约束** | ✅ 关键字段 `spu_id`、`sku_name`、`location_code` 必须 NOT NULL | ✅ **已完成 (Phase 1)** | `SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name IN ('products', 'inventory_batch')` |

---

## 📊 总体进度汇总

| 阶段 | 状态 | 完成度 | 说明 |
|-----|------|-------|------|
| **Phase 1: 数据库 & 实体层** | ✅ **已完成** | 100% | 已执行迁移脚本，所有表结构、字段、索引、约束已就位 |
| **Phase 2: 业务逻辑 & API** | ⏳ **进行中** | 0% | 需实现 Service、Controller、Repository 自定义方法 |
| **Phase 3: 前端集成 & 测试** | ⏳ **未开始** | 0% | 需实现前端页面、集成测试、性能测试 |

---

## ✅ Phase 1 验收清单（已完成项）

以下项目已在数据库迁移中完成，可直接验证：

```sql
-- ✅ 1. 验证 product_spu 表存在
SELECT COUNT(*) FROM information_schema.tables WHERE table_name = 'product_spu';
-- 预期: 1

-- ✅ 2. 验证默认 SPU 存在
SELECT * FROM product_spu WHERE id = 0 AND spu_code = 'DEFAULT-SPU';
-- 预期: 1 行

-- ✅ 3. 验证 products 表新增字段
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'products'
  AND column_name IN ('spu_id', 'sku_name', 'specs');
-- 预期: 3 行

-- ✅ 4. 验证 inventory_batch 表新增字段
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'inventory_batch'
  AND column_name = 'location_code';
-- 预期: 1 行

-- ✅ 5. 验证外键约束
SELECT conname FROM pg_constraint WHERE conname = 'fk_product_spu';
-- 预期: 1 行

-- ✅ 6. 验证 batch_code 唯一约束已删除
SELECT COUNT(*)
FROM pg_constraint
WHERE conrelid = 'inventory_batch'::regclass
  AND contype = 'u'
  AND pg_get_constraintdef(oid) LIKE '%batch_code%';
-- 预期: 0

-- ✅ 7. 验证索引
SELECT indexname
FROM pg_indexes
WHERE tablename IN ('products', 'inventory_batch', 'product_spu')
  AND indexname IN ('idx_spu_id', 'idx_batch_code', 'idx_batch_location', 'idx_location_code');
-- 预期: 4 行

-- ✅ 8. 验证数据迁移完整性
SELECT
    COUNT(*) AS total_products,
    COUNT(*) FILTER (WHERE spu_id IS NULL) AS null_spu_id,
    COUNT(*) FILTER (WHERE sku_name IS NULL OR sku_name = '') AS null_sku_name
FROM products;
-- 预期: total_products > 0, null_spu_id = 0, null_sku_name = 0
```

---

## 🚀 下一步行动建议

### 优先级 P0（核心功能，必须实现）

1. **实现 ProductSpuRepository & ProductSpuService**
   - 创建 `ProductSpuRepository.java`
   - 创建 `ProductSpuService.java`（包含 `createSpu()`、`updateSpu()`、`listSpus()` 方法）

2. **实现 BatchCodeService（Hashids 批次码生成）**
   - 配置 `Hashids` bean
   - 实现 `generateBatchCode(Long itemId)` 和 `decodeBatchCode(String code)` 方法

3. **完善 PurchaseOrderService 三阶段流程**
   - 实现 `confirmAsn()` 方法（生成批次码，状态变更为 IN_TRANSIT）
   - 实现 `receive()` 方法（分配库位，状态变更为 COMPLETED）
   - 实现 `rollbackToOrdering()` 方法（状态回退，作废批次）

4. **实现 InventoryService FIFO 出库逻辑**
   - 实现 `adjustStock()` 方法的 FIFO 策略
   - 实现零头优先逻辑

### 优先级 P1（重要功能，尽快实现）

5. **实现所有 Controller API 端点**
   - `ProductSpuController`（CRUD）
   - `PurchaseOrderController`（三阶段 API）
   - `InventoryController`（入库、出库、查询）

6. **单元测试 & 集成测试**
   - 测试 SPU-SKU 关联逻辑
   - 测试批次码生成唯一性
   - 测试 FIFO 出库策略
   - 测试并发扣减（乐观锁）

### 优先级 P2（增强功能，后续优化）

7. **前端集成**
   - SPU 管理页面
   - 采购单三阶段操作界面
   - 批次码扫描入库界面

8. **性能优化**
   - 库存聚合查询缓存
   - 批次查询索引优化

---

## 📝 备注

- **SPU-SKU 架构标记**：所有涉及 `spu_id`、`sku_name`、`product_spu` 表的功能点都标注了 🔑
- **多库位管理标记**：所有涉及 `location_code`、`batch_code` 非唯一、跨库位查询的功能点都标注了 🔑
- **状态说明**：
  - ✅ **已完成 (Phase 1)**：数据库层已就位，无需额外代码
  - ⏳ **待实现 (Phase 2)**：需要编写 Java 代码（Service/Controller/Repository）
  - ⏳ **待验证 (配置)**：需检查配置文件
  - ⏳ **待实现 (Phase 3)**：前端或高级功能

---

**生成工具**: Claude Code
**数据来源**: DEVELOPMENT_GUIDE.md (Section 6 & 7)
**验证基准**: V3.3_Migration_Complete_Idempotent.sql 执行结果
