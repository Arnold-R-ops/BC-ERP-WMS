# 创建 PostgreSQL 测试数据库详细步骤

## 方法 1：使用 pgAdmin 图形界面（推荐，最简单）

### 第 1 步：启动 pgAdmin
1. 在 Windows 开始菜单中搜索 **pgAdmin**
2. 点击打开 **pgAdmin 4**
3. 等待 pgAdmin 在浏览器中启动

### 第 2 步：连接到 PostgreSQL 服务器
1. 在左侧树形菜单中，找到 **Servers**
2. 展开 **Servers** → 找到 **PostgreSQL 16**（或您安装的版本）
3. 点击 **PostgreSQL 16**
4. 如果弹出密码输入框，输入您的 PostgreSQL 密码：`123465`
5. 等待连接成功

### 第 3 步：创建测试数据库
1. 在左侧树形菜单中，右键点击 **Databases**
2. 选择 **Create** → **Database...**
3. 在弹出的窗口中填写：
   - **Database（数据库名）**: `wms_db_test`
   - **Owner（所有者）**: `postgres`（默认）
   - **Encoding（编码）**: `UTF8`（默认）
4. 点击 **Save** 保存

### 第 4 步：验证数据库创建成功
1. 在左侧 **Databases** 下，应该能看到新创建的 `wms_db_test`
2. 如果看到了，说明创建成功 ✅

---

## 方法 2：使用 psql 命令行

### 第 1 步：打开命令提示符
1. 按 `Win + R`
2. 输入 `cmd`
3. 按 `Enter`

### 第 2 步：连接到 PostgreSQL
在命令行中输入：

```bash
psql -U postgres -h localhost
```

然后输入密码：`123465`

### 第 3 步：创建数据库
连接成功后，执行以下 SQL 命令：

```sql
CREATE DATABASE wms_db_test
    WITH OWNER = postgres
    ENCODING = 'UTF8';
```

如果看到 `CREATE DATABASE`，说明创建成功 ✅

### 第 4 步：验证数据库
执行命令：

```sql
\l
```

应该能在列表中看到 `wms_db_test`。

### 第 5 步：退出 psql
```sql
\q
```

---

## 方法 3：使用 SQL 脚本文件

### 第 1 步：打开命令提示符
按 `Win + R`，输入 `cmd`，按 `Enter`

### 第 2 步：执行 SQL 脚本
在命令行中输入（注意路径）：

```bash
psql -U postgres -h localhost -f "D:\ERP_WMS\BC warehouse\2G\create-test-database.sql"
```

输入密码：`123465`

如果看到 `✓ 测试数据库 wms_db_test 创建成功！`，说明创建成功 ✅

---

## ⚠️ 常见问题

### 问题 1：找不到 pgAdmin

**解决方案**：
1. 检查是否安装了 PostgreSQL（安装时会自动安装 pgAdmin）
2. 如果没有 pgAdmin，可以使用命令行方法（方法 2）

---

### 问题 2：psql 命令不存在

**错误提示**：`'psql' 不是内部或外部命令...`

**解决方案**：添加 PostgreSQL 到系统环境变量

**步骤 1**：找到 PostgreSQL 安装路径，通常在：
```
C:\Program Files\PostgreSQL\16\bin
```

**步骤 2**：添加到系统 PATH
1. 右键 **此电脑** → **属性**
2. 点击 **高级系统设置**
3. 点击 **环境变量**
4. 在 **系统变量** 中找到 **Path**，双击
5. 点击 **新建**
6. 添加：`C:\Program Files\PostgreSQL\16\bin`
7. 点击 **确定** → **确定** → **确定**
8. **重新打开命令提示符**，再试一次

---

### 问题 3：密码认证失败

**错误提示**：`password authentication failed for user "postgres"`

**解决方案 A**：确认密码
- 如果您的密码不是 `123465`，请输入正确的密码

**解决方案 B**：重置密码

**步骤 1**：以管理员身份打开命令提示符

**步骤 2**：找到 PostgreSQL 数据目录下的 `pg_hba.conf` 文件
通常在：`C:\Program Files\PostgreSQL\16\data\pg_hba.conf`

**步骤 3**：修改认证方式（临时）
将以下行：
```
host    all             all             127.0.0.1/32            scram-sha-256
```
改为：
```
host    all             all             127.0.0.1/32            trust
```

**步骤 4**：重启 PostgreSQL 服务
```bash
net stop postgresql-x64-16
net start postgresql-x64-16
```

**步骤 5**：连接并修改密码
```bash
psql -U postgres -h localhost
ALTER USER postgres WITH PASSWORD '123465';
\q
```

**步骤 6**：恢复 `pg_hba.conf` 配置
将 `trust` 改回 `scram-sha-256`，再重启服务

---

### 问题 4：PostgreSQL 服务未启动

**错误提示**：`could not connect to server: Connection refused`

**解决方案**：启动 PostgreSQL 服务

**方法 1**：使用命令行
```bash
net start postgresql-x64-16
```

**方法 2**：使用服务管理器
1. 按 `Win + R`
2. 输入 `services.msc`
3. 找到 **postgresql-x64-16**
4. 右键 → **启动**

---

### 问题 5：数据库已存在

**错误提示**：`ERROR: database "wms_db_test" already exists`

**这是好消息！** 说明数据库已经创建好了，可以直接使用 ✅

如果需要重新创建（删除旧数据），可以：

**使用 pgAdmin**：
1. 右键 `wms_db_test` → **Delete/Drop**
2. 确认删除
3. 重新创建

**使用 psql**：
```sql
DROP DATABASE wms_db_test;
CREATE DATABASE wms_db_test WITH OWNER = postgres ENCODING = 'UTF8';
```

---

## ✅ 验证数据库创建成功

### 方法 1：使用 pgAdmin
在左侧菜单中，展开 **Databases**，应该能看到 `wms_db_test`

### 方法 2：使用 psql
```bash
psql -U postgres -h localhost -c "\l" | findstr wms_db_test
```

如果显示 `wms_db_test`，说明创建成功 ✅

---

## 🎯 创建完成后

数据库创建成功后，您可以：

1. ✅ 在 IDE 中刷新 Maven 项目
2. ✅ 运行测试：
   - `CustomUserDetailsServiceTest`（单元测试）
   - `AuthControllerIntegrationTest`（集成测试）

---

**提示**：推荐使用 **方法 1（pgAdmin）**，最简单直观！
