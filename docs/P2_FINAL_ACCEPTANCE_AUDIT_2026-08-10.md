# P2 全部范围最终验收与发布前只读审查

审查日期：2026-08-10

## 结论

用户当前确认的 P2 功能范围和暂定普通业务角色权限基线全部通过最终验收，未发现功能或安全回归阻塞项。

本结论表示“功能验收及发布分支 CI 通过”，不表示已经正式发布。本轮没有调用开发库业务写接口，没有修改库存、预留、采购、销售、仓库作业或 IAM 业务数据；已按用户授权完成 Git 提交和推送，但没有创建标签或部署。

标准 Maven/CI、工作区变更分组和分支推送已经完成；正式发布仍需完成历史凭证轮换确认，并取得创建标签或部署的单独授权。

## 最终证据

| 核验项 | 结果 | 证据 |
|---|---|---|
| 当前服务 | 通过 | PID 50224 运行 `wms-system-v449-codex-clean3.jar`；`GET /health/check` 返回 HTTP 200、`UP` |
| Flyway | 通过 | 当前服务启动日志确认 schema 为 `4.49`；V4.49 已完成普通业务模板精确基线和受影响用户安全版本推进 |
| Java 主源码编译 | 通过 | 无 Maven 环境下使用本机依赖缓存独立编译 361 个当前主源码文件，错误 0 |
| Java 测试源码编译 | 通过 | 独立编译 118 个当前测试源码文件，错误 0 |
| Java 默认回归 | 通过 | 113 个测试类、1231 项，失败 0 |
| Java 扩展集成回归 | 通过 | 2 个非标准命名集成类、58 项，失败 0 |
| Java 合计 | 通过 | 1289 项，失败 0 |
| 前端回归 | 通过 | 44 个测试文件、142 项，失败 0 |
| 前端构建 | 通过 | `tsc --noEmit && vite build` 成功 |
| OpenAPI | 通过 | 实时 `/v3/api-docs` 与 `docs/frontend/openapi.json` 递归排序后语义一致 |
| TypeScript 契约 | 通过 | 从 OpenAPI 快照重新生成的类型与 `frontend/src/api/schema.d.ts` 逐行一致 |
| HTTP 历史验收集 | 通过 | 两套清单、哈希、脚本和健康检查预检全部通过；识别 25+21 个 POST，`businessWritesExecuted=false` |
| 工作区格式 | 通过 | `git diff --check` 无空白错误；仅有既有 LF/CRLF 提示 |
| 当前配置密钥 | 通过 | 生产配置中的密码/JWT 项均使用环境变量占位；固定测试值只存在于测试资源 |

## 审查中修正

扩展集成套件的采购确认和收货场景仍使用权限拆分前的模拟凭据，导致其准备步骤创建采购单时被后端正确拒绝为 403。测试已改为显式携带：

- 确认场景：`purchase:create + purchase:confirm`。
- 收货场景：`purchase:create + purchase:confirm + purchase:receive`。

修正后扩展集成回归 58/58 通过。该问题属于测试契约过期，不是生产鉴权绕过或业务接口缺陷。

根目录 `.gitignore` 已补充 `node_modules/`，避免依赖目录被误加入发布变更；该目录不再出现在 Git 未跟踪清单中。

## 功能范围结论

- 当前激活角色鉴权、禁用角色继承截断、权限缓存与旧 JWT 失效：通过。
- 受保护特权角色、系统保留权限、普通业务模板复制和自定义包简易审批：通过。
- 权限申请、审批隔离、角色/仓库原子授予与撤销及不可变审计：通过。
- `WAREHOUSE_STAFF` 仓库数据范围和最小页面范围：通过。
- `GENERAL_MANAGER=40`、`SALESPERSON=15`、`WAREHOUSE_ADMIN=21`、`WAREHOUSE_STAFF=8` 暂定精确基线：通过。
- 采购单 `ORDERING` 编辑、历史测试数据归档、库存零影响和归档后销售事实刷新：通过。
- 前端菜单、路由和主要业务按钮按当前有效权限码控制，后端保持最终鉴权权威：通过。

## 发布门禁

功能验收和发布分支 CI 已通过，但正式发布尚未执行：

1. 244 个发布入口已按三组提交为 `121e8e9`、`29fae47`、`57db5ad` 并推送到远程分支 `2-1G`。
2. GitHub Actions 运行 `31393616581` 已在 Java 17 + PostgreSQL 18 环境完成后端 Maven 验证，并完成前端测试、构建、OpenAPI 类型漂移检查和 Release gate；三个任务均为 `success`。
3. Flyway 9.22.3 对 PostgreSQL 18.1 的版本支持警告仍需单独升级计划。
4. 历史凭证扫描已经完成：无高置信私钥/平台令牌待复核，但当前数据库密码仍匹配 Git 历史旧值，且数据库账号未显式切换为 `wms_app`，发布凭证门禁为 `BLOCKED`。详见 `docs/P2_CREDENTIAL_HISTORY_AUDIT_2026-08-10.md`。
5. 未获得创建标签或部署授权，因此不得宣称已经正式发布。

## 已确认后续版本事项

全部验收完成后，普通业务模板与受保护特权角色的分类、权限矩阵和保护策略进入后续专项版本。专项修改必须重新确认规则、使用新 Flyway 迁移、推进受影响用户安全版本，并重新执行越权和真实 API 验收；不得回写既有自定义权限包快照。

## 下一任务

上述发布变更分组、三组提交、推送、远程 CI 和历史凭证扫描均已完成。下一任务是在用户明确批准的维护窗口执行数据库密码轮换和 `wms_app` 低权限账号切换，再重新运行凭证门禁；在此之前不得创建 P2 标签或部署。按 `CODEX_TASK_MODEL_POLICY` 建议使用 **GPT-5.6 Sol + XHigh（L5）**。
