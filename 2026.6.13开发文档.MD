# WMS 后端开发文档

**文档名称：** 2026.6.13开发文档
**编写日期：** 2026-06-13
**最新更新：** 2026-06-14
**代码范围：** `D:\ERP_WMS\BC warehouse\2G`
**接口测试范围：** `D:\ERP_WMS\BC warehouse\wms-test-http`
**当前版本重点：** V4.4 核心业务闭环、企业级 IAM、用户逻辑删除与安全会话失效

## 文档更新记录

| 日期 | 更新内容 |
|---|---|
| 2026-06-13 | 建立 V4.4 功能、业务逻辑和用户说明基线 |
| 2026-06-14 | 增加企业级 IAM、兼容性迁移、用户逻辑删除、安全保护、旧 Token 失效和全路由回归结果 |

---

# 用户说明手册

## U1. 程序简介

本程序是一套面向批发、仓储和库存管理场景的 WMS 后端系统。它以“商品、仓库、库位和批次”为库存管理基础，覆盖采购入库、销售出库、库存查询、盘点平账、权限控制和补货预测等业务。

系统当前以 REST API 形式提供能力，主要供管理后台、仓库终端、移动扫码程序或第三方平台调用。未来前端中的按钮和页面名称可以不同，但其业务规则应与本文一致。

系统的主要特点：

- 支持多个仓库和多个库位。
- 按商品批次、生产日期和有效期追踪库存。
- 支持 FEFO，即有效期越近的商品越优先出库。
- 支持整箱和散件的智能分配。
- 支持入库审批、销售审批和出库确认。
- 支持库存盘点、盘盈盘亏和审计流水。
- 支持用户、角色和接口级权限控制。
- 支持 Excel 导入以及 Shopify 订单同步基础能力。
- 支持低库存分析和补货建议。
- 使用事务和乐观锁防止重复扣减及部分提交。

---

## U2. 适用用户

| 用户类型 | 主要工作 |
|---|---|
| 系统管理员 | 维护用户、角色、权限和基础配置 |
| 仓库主管 | 维护仓库与库位，审核盘点，处理库存异常 |
| 仓库人员 | 收货、上架、查询库存、拣货和盘点 |
| 采购人员 | 创建采购或入库计划，确认批次和预计到货信息 |
| 审批人员 | 审批入库计划、销售订单和异常库存处理 |
| 销售人员 | 创建客户与销售订单，查看订单履约状态 |
| 管理人员 | 查看库存摘要、补货建议和业务状态 |
| 集成程序 | 通过 JWT 调用 API，或同步 Shopify 等外部订单 |

具体可执行操作由当前用户角色及数据库权限共同决定。`SUPER_ADMIN` 拥有系统最高权限。

---

## U3. 功能总览

### U3.1 用户与安全

用户可以登录系统并获得 JWT。后续业务请求需要携带 Token，系统会根据当前角色和权限决定是否允许访问。

主要能力：

- 用户登录。
- 用户创建、修改和逻辑删除。
- 角色分配与移除。
- 多角色身份切换。
- 接口级权限控制。
- 未登录或无权限访问拦截。
- 禁止管理员删除自己的账号。
- 禁止删除系统中最后一个有效的 `SUPER_ADMIN`。
- 用户删除后立即剥夺全部角色，并使旧 JWT 无法继续访问系统。

用户删除采用逻辑删除，不会直接从数据库抹除用户主体。系统会保留用户 ID 和历史关联，确保入库单、销售单、库存流水等历史记录仍可追溯；同时会匿名化账号、禁用登录并清除角色权限。

### U3.2 仓库与库位

用户可以创建多个仓库，并在仓库中建立区域、货架和位置。库存最终存放在具体库位中。

主要能力：

- 新建和修改仓库。
- 启用或停用仓库。
- 创建和维护库位。
- 启用或停用库位。
- 查询仓库中的全部库位或空库位。
- 查询仓库已有库位数量。

### U3.3 商品与客户

商品是库存、采购和销售的基础。客户是销售订单的购买方。

商品可维护：

- 条码、名称、SPU、SKU 和规格。
- 售价及最低销售价。
- 整箱数量和包装单位。
- 安全库存、最低库存和采购提前期。
- 临期判断天数。

客户可维护：

- 客户编码和名称。
- 联系人、电话、邮箱和地址。
- 信用额度和启用状态。

### U3.4 采购与入库

系统提供两类入库业务：

1. **入库单流程：** 适合申请、审批、采购确认和仓库收货的业务。
2. **采购订单流程：** 适合记录采购、在途、分批收货和采购回退。

入库完成前，货物不会计入可销售现货。只有仓库完成收货后，系统才创建实际库存批次。

### U3.5 销售订单

销售人员可以先录入客户需求。订单创建成功不代表库存已经承诺。

系统会根据订单金额和销售价格决定：

- 自动审批并进入待发货。
- 转为待审批，由经理人工处理。
- 审批时库存不足则拒绝分配。

订单还支持修改、驳回、业务取消和系统作废。

### U3.6 智能出库

销售订单获得批准后，系统根据库存批次生成一个或多个出库任务。

分配原则：

- 用户指定批次时优先使用指定批次。
- 客户拒收临期商品时过滤临期批次。
- 默认按 FEFO 优先使用有效期更近的批次。
- 非整箱需求优先消耗已有散件。
- 再分配整箱库存。
- 必要时拆箱满足剩余数量。

仓库人员确认任务后才真正扣减库存。全部任务完成后，销售订单自动变为已发货。

### U3.7 库存与批次

系统可以按商品、仓库、库位和批次查询库存。

主要能力：

- 查询商品总库存。
- 查询商品的全部有效批次。
- 查询指定库位的库存。
- 查询指定批次。
- 查询临期、过期和耗尽批次。
- 查看库存摘要、SKU 明细和库位视图。
- 对库存或指定批次执行受控调整。

每个批次保存数量、库位、生产日期、有效期和入库时间，便于食品追溯。

### U3.8 盘点

盘点用于处理系统账面数量与仓库实物数量不一致的问题。

主要能力：

- 创建月度、季度、年度或临时盘点任务。
- 创建任务时保存库存快照。
- 仓库人员录入实盘数量。
- 自动计算盘盈或盘亏。
- 主管审核差异。
- 审核通过后自动调整库存并生成流水。
- 审核不通过时退回重新盘点。

### U3.9 库存预测

系统根据历史出库流水、当前库存、安全库存和采购提前期生成补货建议。

用户可以查看：

- 单个商品的补货建议。
- 全部低库存商品。
- 高优先级和紧急补货商品。
- 日均出库量。
- 建议采购数量。
- 预计断货天数和采购成本。

### U3.10 Excel 与外部平台

系统支持：

- 下载销售订单 Excel 模板。
- 通过 Excel 导入销售订单。
- 通过 Excel 导入采购订单。
- 定时同步已配置的 Shopify 订单。
- 匹配客户和商品，并防止重复同步。

---

## U4. 首次使用顺序

建议按以下顺序初始化系统：

1. 使用管理员账号登录。
2. 创建业务用户并分配角色。
3. 创建仓库。
4. 为仓库创建实际库位。
5. 创建或导入商品。
6. 创建客户和供应商基础数据。
7. 通过采购单或入库单录入初始库存。
8. 完成审批、确认和仓库收货。
9. 查询库存批次，确认数量、库位和日期正确。
10. 开始创建销售订单并执行出库。

没有仓库、库位、商品和实际库存时，销售出库流程无法完整运行。

---

## U5. 常用业务操作

### U5.1 完成一次入库

```text
创建入库单
  → 总经理审批计划
  → 采购员确认数量、批次日期和库位
  → 仓库实际收货
  → 库存批次生成
```

操作时应重点检查：

- 供应商和商品是否正确。
- 计划数量和确认数量是否合理。
- 目标仓库和库位是否真实存在。
- 生产日期不能晚于有效期。
- 收货数量应与现场实物一致。

### U5.2 完成一次销售出库

```text
创建销售订单
  → 自动审批或经理审批
  → 系统执行 FEFO 和整散分配
  → 生成出库任务
  → 仓库确认每个任务
  → 扣减库存
  → 销售订单变为 SHIPPED
```

如果库存不足：

- 订单可以先作为业务需求保存。
- 需要审批的订单会在审批和分配阶段被拦截。
- 系统不会生成负数库存。
- 应补充入库、修改订单或取消订单。

### U5.3 处理账实不符

```text
创建临时盘点任务
  → 开始盘点
  → 录入实物数量
  → 提交复核
  → 主管审核
  → 自动调整库存并生成流水
```

不建议绕过盘点直接修改数据库，也不建议使用负库存代替真实库存校正。

### U5.4 查看库存风险

建议日常关注：

- 低于安全库存的商品。
- 即将过期的批次。
- 已经过期但仍然有效的批次。
- 库存为零或已耗尽的批次。
- 长期未完成的出库任务。
- 待审批和待收货订单。
- 紧急补货建议。

### U5.5 安全删除用户

管理员删除用户时，系统按以下顺序处理：

```text
校验目标用户存在
  → 禁止删除当前操作者自己
  → 禁止删除最后一个有效 SUPER_ADMIN
  → 匿名化用户名和显示名称
  → 禁用账号并重置密码
  → 清除默认角色和全部角色关联
  → 标记为逻辑删除
  → 清理权限缓存
  → 旧 JWT 后续请求被拒绝
```

逻辑删除后的用户不会出现在普通用户列表中，也不能重新登录。用户主体记录仍保留，用于历史单据和审计追溯。

---

## U6. 主要状态说明

### U6.1 入库单

| 状态 | 含义 |
|---|---|
| `PENDING_APPROVAL` | 已创建，等待审批 |
| `APPROVED_PLAN` | 入库计划已批准 |
| `AWAITING_RECEIVAL` | 已确认批次信息，等待仓库收货 |
| `COMPLETED` | 收货完成，库存已入账 |
| `REJECTED` | 入库计划被驳回 |

### U6.2 采购订单

| 状态 | 含义 |
|---|---|
| `ORDERING` | 采购单已创建，等待确认 |
| `IN_TRANSIT` | 已确认采购，货物在途 |
| `PARTIALLY_RECEIVED` | 已收到部分货物 |
| `COMPLETED` | 全部货物已入库 |
| `CANCELLED` | 真实业务取消 |
| `VOIDED` | 错误或测试数据作废 |

### U6.3 销售订单

| 状态 | 含义 |
|---|---|
| `DRAFT` | 草稿 |
| `PENDING_APPROVAL` | 等待经理审批 |
| `APPROVED_AWAITING_SHIPMENT` | 已批准并生成出库任务 |
| `SHIPPED` | 全部出库任务完成 |
| `REJECTED` | 审批被拒绝 |
| `CANCELLED` | 真实业务取消 |
| `VOIDED` | 错误、测试或噪音数据作废 |

### U6.4 出库任务

| 状态 | 含义 |
|---|---|
| `PENDING` | 等待拣货 |
| `PICKING` | 正在拣货 |
| `COMPLETED` | 已确认出库并扣减库存 |

### U6.5 盘点任务

| 状态 | 含义 |
|---|---|
| `CREATED` | 任务已创建并生成快照 |
| `COUNTING` | 仓库正在盘点 |
| `REVIEWING` | 等待主管复核 |
| `COMPLETED` | 已审核并完成平账 |
| `CANCELLED` | 盘点业务取消 |
| `VOIDED` | 错误或测试任务作废 |

---

## U7. 使用注意事项

1. 所有日期、数量、商品、批次和库位应以现场业务资料为准。
2. 销售订单创建成功不代表库存已经锁定，审批和分配成功后才进入履约。
3. 当前出库任务属于逻辑分配，尚未建立独立的库存预占数量字段。
4. 多订单并发分配时仍需关注重复占用风险，后续版本将增加真实库存预占。
5. 当前不支持 Backorder 自动欠货拆分。
6. 当前不会使用采购在途数量自动承诺销售订单。
7. 当前不允许通过正常出库形成负库存。
8. 发现现场有货但系统无货时，应使用盘点或受控库存调整处理。
9. 不应重复点击已经完成的审批、收货或出库操作；系统会拒绝部分重复操作。
10. `CANCELLED` 用于真实业务取消，`VOIDED` 用于错误、测试或无效数据。
11. 生产环境必须更换数据库密码、JWT Secret 和默认管理员密码。
12. Excel 导入前应校验模板、商品编码和必填字段。
13. 用户删除属于不可直接恢复的安全操作；如需恢复，应由管理员通过专门的恢复流程重新设置账号、密码和角色，不应直接修改删除标志。
14. 删除用户不会删除历史业务单据，但会立即移除其全部角色权限。
15. 当前旧 JWT 失效依赖每次请求重新加载有效用户及权限缓存清理；多节点部署时建议升级为 Redis 黑名单或集中式 Token 版本控制。

---

## U8. 当前版本能力边界

当前 V4.4 已完成并验证：

- 入库审批与收货。
- 双批次库存创建。
- FEFO。
- 整散分离。
- 出库任务生成和确认。
- 批量出库事务回滚。
- 重复确认拦截。
- 超卖审批拦截。
- 无 Token 访问拦截。
- 用户创建兼容旧 `role` 字段。
- 用户逻辑删除。
- 自我删除保护。
- 最后一个 `SUPER_ADMIN` 删除保护。
- 被删除用户旧 JWT 拦截。

尚未作为当前正式能力提供：

- 真实库存预占字段。
- 部分分配和 Backorder。
- 在途库存 ATP 承诺。
- 负库存强制发货。
- 销售缺货后自动关联采购并唤醒。

遇到这些场景时，应采用人工补货、修改订单、盘点调整或等待后续版本实现。

---

## 0. 文档目的

本文档回答两个问题：

1. 当前后端已经具备哪些功能。
2. 当前后端的业务逻辑、状态流转和核心算法是怎样运行的。

文档以 2026 年 6 月 13 日的业务基线和 2026 年 6 月 14 日的 IAM 安全更新为准。文中将明确区分：

- **已实现：** 当前代码已经具备。
- **已验证：** 已通过实际 HTTP 接口测试。
- **部分实现：** 有代码基础，但业务闭环或数据模型还不完整。
- **规划功能：** 当前尚未实现，不应被前端或测试脚本当作现有能力。

---

# 第一部分：当前后端功能

## 1. 技术架构

| 项目 | 当前实现 |
|---|---|
| 开发语言 | Java 17 |
| 应用框架 | Spring Boot 3.2.11 |
| API | Spring MVC REST API |
| ORM | Spring Data JPA / Hibernate |
| 数据库 | PostgreSQL |
| 数据库版本管理 | SQL 迁移脚本；Flyway 自动执行链路尚需部署基线化 |
| 认证 | Spring Security + JWT |
| 授权 | 数据库动态 RBAC + `@PreAuthorize` |
| 密码加密 | BCrypt |
| 本地权限缓存 | Caffeine |
| 并发控制 | JPA `@Version` 乐观锁、Spring Retry |
| Excel | Apache POI |
| 批次码 | Hashids、SPU-SKU-DATE 规则 |
| 事务 | Spring `@Transactional` |
| 测试 | JUnit 5、Mockito、MockMvc、HTTP Client |

系统采用典型分层结构：

```text
Controller
    ↓
Service
    ↓
Repository
    ↓
PostgreSQL
```

主要业务对象：

```text
仓库 Warehouse
  └─ 库位 Location
       └─ 库存批次 InventoryBatch

商品 Product
客户 Customer
供应商 Supplier

入库单 InboundOrder
采购单 PurchaseOrder
销售单 SalesOrder
出库任务 OutboundTask
库存流水 StockTransaction
盘点任务 StocktakeTask
```

---

## 2. 认证与权限功能

### 2.1 已实现功能

| 接口 | 功能 |
|---|---|
| `POST /api/auth/login` | 用户名密码登录并签发 JWT |
| `POST /api/auth/switch-role` | 在用户已有角色之间切换当前身份 |
| `GET /api/auth/health` | 认证模块健康检查 |
| `GET /api/users` | 查询用户 |
| `POST /api/users` | 创建用户 |
| `PUT /api/users/{id}` | 修改用户 |
| `DELETE /api/users/{id}` | 逻辑删除用户并立即撤销权限 |
| `POST /api/users/{id}/roles` | 为用户分配角色 |
| `DELETE /api/users/{id}/roles/{roleId}` | 移除用户角色 |

### 2.2 权限模型

- JWT 中保存用户名、当前角色和可切换角色。
- 系统不使用 HTTP Session，服务端保持无状态。
- `sys_user_role` 是权限判定的唯一事实来源。
- `users.role` 仅为兼容历史数据而保留，已标记为废弃，不再参与新权限判定。
- 动态权限管理器根据“请求方法 + URI”匹配数据库权限。
- `SUPER_ADMIN` 可以绕过普通权限检查。
- Controller 层进一步使用 `@PreAuthorize` 校验业务权限。
- 权限数据支持角色、权限、角色继承和本地缓存。

### 2.3 公开接口

当前明确公开的路径包括：

```text
/api/auth/login
/health/**
/actuator/**
/error
/favicon.ico
```

其他业务接口原则上必须携带有效 JWT。

### 2.4 用户逻辑删除模型

用户删除采用逻辑删除和权限物理撤销相结合的方式：

| 数据 | 删除策略 | 原因 |
|---|---|---|
| `users` 用户主体 | 逻辑删除 | 保留历史订单、库存流水和审计关系 |
| `sys_user_role` 角色关联 | 物理删除 | 立即剥夺全部系统权限 |
| 用户名、显示名称和密码 | 匿名化或重置 | 防止隐私泄露和账号再次登录 |
| 权限缓存 | 事务提交后清理 | 防止旧权限在缓存中继续生效 |

用户表新增或启用以下字段：

```text
is_deleted  是否逻辑删除
deleted_at  删除时间
deleted_by  执行删除的管理员 ID
```

普通 ORM 查询默认过滤 `is_deleted = true` 的用户。因此，已删除用户不会出现在用户列表、用户详情和登录查询中。

### 2.5 删除安全保护

`DELETE /api/users/{id}` 在同一个事务中执行以下保护：

1. 校验目标用户存在。
2. 拒绝当前用户删除自己。
3. 如果目标用户拥有 `SUPER_ADMIN`，检查系统是否仍有其他有效超级管理员。
4. 最后一个有效 `SUPER_ADMIN` 不允许删除。
5. 匿名化并禁用用户。
6. 删除其全部 `sys_user_role` 关联。
7. 保存逻辑删除审计信息。
8. 事务提交成功后清理权限缓存。

任一步骤失败，整个删除操作回滚，不会出现“用户已禁用但角色未清除”或“角色已清除但用户状态未更新”的半完成状态。

### 2.6 旧 Token 失效机制

当前 JWT 为无状态 Token，没有使用 Redis 会话存储。系统采用以下安全闭环：

```text
客户端携带旧 JWT
  → JwtAuthenticationFilter 解析用户名
  → CustomUserDetailsService 重新查询有效用户
  → ORM 自动过滤 is_deleted = true
  → 已删除用户无法加载
  → 请求返回 401 或 403
```

同时，删除事务提交后会清理该用户的本地权限缓存。单实例部署已经能够立即阻止旧 Token；未来多实例部署建议增加 Redis Token 黑名单或 `token_version`，实现跨节点即时失效。

---

## 3. 仓库与库位管理

### 3.1 仓库功能

| 接口 | 功能 |
|---|---|
| `GET /api/warehouses` | 查询全部仓库 |
| `GET /api/warehouses/active` | 查询启用仓库 |
| `GET /api/warehouses/{id}` | 按 ID 查询仓库 |
| `GET /api/warehouses/code/{code}` | 按编码查询仓库 |
| `POST /api/warehouses` | 创建仓库 |
| `PUT /api/warehouses/{id}` | 修改仓库 |
| `PUT /api/warehouses/{id}/activate` | 启用仓库 |
| `PUT /api/warehouses/{id}/deactivate` | 停用仓库 |
| `GET /api/warehouses/{id}/location-count` | 查询仓库库位数量 |

### 3.2 库位功能

| 接口 | 功能 |
|---|---|
| `GET /api/locations/{id}` | 查询库位 |
| `GET /api/locations/warehouse/{warehouseId}` | 查询仓库全部库位 |
| `GET /api/locations/warehouse/{warehouseId}/empty` | 查询空库位 |
| `POST /api/locations` | 创建库位 |
| `PUT /api/locations/{id}` | 修改库位备注等信息 |
| `PUT /api/locations/{id}/enable` | 启用库位 |
| `PUT /api/locations/{id}/disable` | 停用库位 |

库位编码由仓库、区域、货架和位置组成，例如：

```text
WH-V44-1781352541-ZONE_A-A-01
```

---

## 4. 商品与客户主数据

### 4.1 商品功能

| 接口 | 功能 |
|---|---|
| `POST /api/products` | 创建商品 |
| `GET /api/products` | 查询商品列表 |
| `GET /api/products/{id}` | 查询商品详情 |
| `PUT /api/products/{id}` | 修改商品 |

商品支持的主要业务字段：

- 条码、名称、SPU、SKU、规格。
- 标准售价和最低销售价。
- 最低库存、安全库存和采购提前期。
- 每箱数量、换算率和包装单位。
- 临期阈值。
- 启用状态。

### 4.2 客户功能

| 接口 | 功能 |
|---|---|
| `POST /api/customers` | 创建客户 |
| `GET /api/customers` | 查询客户列表 |
| `GET /api/customers/{id}` | 查询客户详情 |
| `PUT /api/customers/{id}` | 修改客户 |
| `DELETE /api/customers/{id}` | 删除客户 |

客户数据包含编码、联系人、电话、邮箱、地址、信用额度和启用状态。

---

## 5. 入库单功能

| 接口 | 功能 |
|---|---|
| `POST /api/inbound-orders` | 创建入库申请 |
| `POST /api/inbound-orders/{id}/approve-plan` | 总经理审批入库计划 |
| `POST /api/inbound-orders/{id}/confirm-order` | 采购员确认数量、日期和目标库位 |
| `POST /api/inbound-orders/{id}/receive-goods` | 仓库实际收货 |
| `POST /api/inbound-orders/{id}/reject` | 驳回入库单 |
| `GET /api/inbound-orders/{id}` | 查询入库单 |
| `GET /api/inbound-orders/by-order-no/{orderNo}` | 按单号查询 |
| `GET /api/inbound-orders/by-status/{status}` | 按状态查询 |
| `GET /api/inbound-orders/my-orders` | 查询当前用户申请单 |
| `GET /api/inbound-orders/pending-approval` | 查询待审批单 |
| `GET /api/inbound-orders/pending-confirmation` | 查询待采购确认单 |
| `GET /api/inbound-orders/pending-receival` | 查询待收货单 |

入库功能支持：

- 一张入库单包含多个商品明细。
- 每个明细指定仓库、库位、计划数量和成本。
- 确认阶段填写生产日期、有效期和实际确认数量。
- 收货阶段创建库存批次并生成库存流水。
- 支持一个订单产生多个不同批次。
- 全流程写入审计日志。

---

## 6. 采购订单功能

| 接口 | 功能 |
|---|---|
| `POST /api/purchase-orders` | 创建采购单 |
| `POST /api/purchase-orders/upload` | Excel 导入采购单 |
| `PUT /api/purchase-orders/{id}/confirm` | 确认采购单并生成批次码 |
| `PUT /api/purchase-orders/{id}/receive` | 采购收货 |
| `PUT /api/purchase-orders/{id}/rollback` | 从在途状态回退 |
| `GET /api/purchase-orders/{id}` | 查询采购单 |
| `GET /api/purchase-orders` | 查询采购单列表 |

采购订单支持：

- 预计到货日期。
- 三阶段采购流程。
- 分批收货。
- 收货数量累计。
- 批次码提前生成。
- 在途状态回退并作废相关批次。
- Excel 批量导入。

当前采购订单与销售订单之间尚未形成自动的“在途库存承诺”闭环。

---

## 7. 销售订单功能

| 接口 | 功能 |
|---|---|
| `GET /api/sales-orders/template` | 下载销售订单 Excel 模板 |
| `POST /api/sales-orders/upload` | Excel 导入销售订单 |
| `POST /api/sales-orders/batch-options` | 查询可选库存批次 |
| `POST /api/sales-orders` | 创建销售订单 |
| `GET /api/sales-orders` | 查询销售订单 |
| `GET /api/sales-orders/{id}` | 查询销售订单详情 |
| `PUT /api/sales-orders/{id}` | 修改未生效订单 |
| `POST /api/sales-orders/{id}/approve` | 审批订单 |
| `POST /api/sales-orders/{id}/reject` | 驳回订单 |
| `POST /api/sales-orders/{id}/cancel` | 业务取消 |
| `POST /api/sales-orders/{id}/void` | 系统作废 |

销售订单支持：

- 客户、商品、数量、单价和备注。
- 商品标价快照。
- 最低销售价检查。
- 大额订单审批。
- 指定批次。
- 客户是否拒收临期商品。
- 自动审批或人工审批。
- 创建出库任务。
- 业务取消和测试数据作废分离。

---

## 8. 智能分配与出库任务

### 8.1 出库任务接口

| 接口 | 功能 |
|---|---|
| `GET /api/outbound-tasks` | 按销售单或状态查询任务 |
| `GET /api/outbound-tasks/{id}` | 查询任务详情 |
| `POST /api/outbound-tasks/{id}/confirm` | 确认单条出库任务 |
| `POST /api/outbound-tasks/batch-confirm` | 在一个事务中批量确认任务 |

### 8.2 已实现分配能力

- FEFO：先过期先出。
- 临期过滤。
- 用户指定批次优先。
- 按商品箱规识别整箱和散件。
- 非整箱订单优先消耗批次中的零散余数。
- 再分配整箱部分。
- 必要时允许拆箱满足剩余数量。
- 一个销售明细可以拆分为多个出库任务。
- 批量确认任一任务失败时整体回滚。
- 重复确认已完成任务返回冲突错误。

V4.4 已实际验证：

```text
批次 A：50
批次 B：100
销售数量：65

生成任务：
Task A = 50
Task B = 15

确认后：
批次 A = 0
批次 B = 85
销售单 = SHIPPED
```

---

## 9. 库存与批次功能

### 9.1 库存批次接口

| 接口 | 功能 |
|---|---|
| `POST /api/inventory/batches/outbound` | 按批次执行出库 |
| `GET /api/inventory/batches/total-stock/{productId}` | 查询商品总库存 |
| `GET /api/inventory/batches/product/{productId}` | 查询商品库存批次 |
| `GET /api/inventory/batches/location/{locationId}` | 查询库位库存 |
| `GET /api/inventory/batches/{batchCode}` | 查询批次 |
| `GET /api/inventory/batches/expired` | 查询过期批次 |
| `GET /api/inventory/batches/expiring-soon` | 查询临期批次 |
| `GET /api/inventory/batches/exhausted` | 查询耗尽批次 |
| `PUT /api/inventory/batches/{batchCode}/adjust` | 调整指定批次库存 |

### 9.2 通用库存接口

| 接口 | 功能 |
|---|---|
| `POST /api/inventory/adjust` | 库存入、出或调整 |
| `GET /api/inventory/total-stock/{productId}` | 查询商品库存合计 |
| `GET /api/inventory/health` | 库存模块健康检查 |

### 9.3 多维库存视图

| 接口 | 功能 |
|---|---|
| `GET /api/inventory/summary` | 分页库存摘要 |
| `GET /api/inventory/details/{skuId}` | SKU 批次明细 |
| `GET /api/inventory/location/{locationCode}` | 库位库存视图 |

多维视图的聚合工作主要由 SQL/JPQL Projection 完成，避免在内存加载全部库存后再统计。

---

## 10. 盘点功能

| 接口 | 功能 |
|---|---|
| `POST /api/stocktake/tasks` | 创建盘点任务 |
| `GET /api/stocktake/tasks` | 查询盘点任务 |
| `GET /api/stocktake/tasks/{id}` | 查询任务详情 |
| `POST /api/stocktake/tasks/{id}/start` | 开始盘点 |
| `GET /api/stocktake/tasks/{id}/items` | 查询盘点项目 |
| `POST /api/stocktake/tasks/{taskId}/items/{itemId}/count` | 录入实盘数 |
| `POST /api/stocktake/tasks/{id}/finish` | 结束计数 |
| `GET /api/stocktake/tasks/{id}/review-items` | 查询复核明细 |
| `POST /api/stocktake/tasks/{id}/review` | 审核并平账 |

盘点支持：

- 月度、季度、年度和临时盘点。
- 创建任务时生成库存快照。
- 盲盘录入。
- 自动计算盘盈、盘亏。
- 全部项目完成后自动进入审核。
- 审核通过后调整批次库存。
- 为盘盈盘亏创建库存流水。
- 审核驳回后返回盘点中状态。

---

## 11. 库存预测功能

| 接口 | 功能 |
|---|---|
| `GET /api/predictions/reorder/{productId}` | 单品补货建议 |
| `GET /api/predictions/reorder` | 全部补货建议 |
| `GET /api/predictions/reorder/urgent` | 紧急补货建议 |
| `GET /api/predictions/health` | 预测模块健康检查 |

预测能力包括：

- 读取最近 N 天出库流水。
- 计算日均出库量。
- 根据采购提前期和安全库存计算建议补货量。
- 估算库存可维持天数。
- 计算预计采购成本。
- 按紧急程度排序。

当前预测代码使用伦敦时区计算业务日期。

---

## 12. Shopify 集成功能

后端存在 Shopify 集成服务和定时任务：

- 每 5 分钟读取启用的 Shopify 配置。
- 拉取 Shopify 订单。
- 匹配或创建客户。
- 根据 SKU 匹配商品。
- 转换为系统销售订单。
- 检查并跳过重复同步订单。
- 记录同步成功、跳过和失败数量。

该能力主要由定时任务触发，目前没有面向业务用户的 Shopify Controller 接口。

---

## 13. 异常处理与审计

系统使用统一 `errorKey + params` 错误结构，例如：

```json
{
  "errorKey": "BATCH_STOCK_INSUFFICIENT",
  "params": {
    "requestedQuantity": 99999,
    "availableQuantity": 100,
    "shortage": 99899
  },
  "status": 400
}
```

主要防御能力：

- DTO 参数校验。
- 状态机校验。
- 库存不足拦截。
- 重复确认拦截。
- 乐观锁。
- 事务回滚。
- 统一错误键。
- 库存流水。
- 订单审计日志。
- 未认证和无权限请求拦截。

---

# 第二部分：当前业务逻辑

## 14. 登录与授权逻辑

```text
用户提交用户名和密码
        ↓
CustomUserDetailsService 查询用户
        ↓
BCrypt 校验密码
        ↓
查询用户拥有的角色
        ↓
签发 JWT
        ↓
后续请求由 JwtAuthenticationFilter 解析
        ↓
DynamicAuthorizationManager 匹配数据库权限
        ↓
Controller 的 @PreAuthorize 再次校验业务权限
```

JWT 过期时间当前为 24 小时。

### 14.1 用户删除与会话失效逻辑

```text
管理员发起删除
        ↓
解析当前操作者 ID
        ↓
校验不能删除自己
        ↓
校验不能删除最后一个 SUPER_ADMIN
        ↓
事务内匿名化、禁用并标记逻辑删除
        ↓
物理删除 sys_user_role 关联
        ↓
提交事务
        ↓
清理用户权限缓存
        ↓
旧 JWT 再次请求时因用户已被过滤而返回 401/403
```

该设计不依赖数据库外键级联删除用户主体，历史业务记录中的用户 ID 仍然有效。

---

## 15. 入库业务逻辑

### 15.1 状态机

```text
PENDING_APPROVAL
        ↓ approve-plan
APPROVED_PLAN
        ↓ confirm-order
AWAITING_RECEIVAL
        ↓ receive-goods
COMPLETED

PENDING_APPROVAL / APPROVED_PLAN
        ↓ reject
REJECTED
```

### 15.2 创建阶段

创建入库单时：

1. 校验供应商。
2. 校验商品。
3. 校验目标仓库和库位。
4. 保存计划数量和采购成本。
5. 状态设为 `PENDING_APPROVAL`。
6. 记录申请人和审计日志。

### 15.3 审批阶段

总经理审批后：

- 状态变为 `APPROVED_PLAN`。
- 保存审批人、审批时间和审批意见。
- 此时不会增加实际库存。

### 15.4 采购确认阶段

采购员确认时：

- 可以调整确认数量。
- 填写生产日期和有效期。
- 确认仓库和库位。
- 生成系统批次码。
- 状态变为 `AWAITING_RECEIVAL`。

### 15.5 仓库收货阶段

仓库收货时：

1. 校验订单状态。
2. 校验明细、数量和库位。
3. 创建或更新 `InventoryBatch`。
4. 增加实际库存。
5. 写入库存入库流水。
6. 更新实收数量。
7. 状态变为 `COMPLETED`。

只有收货完成后，库存才成为可销售的现货。

---

## 16. 采购订单逻辑

### 16.1 状态机

```text
ORDERING
   ↓ confirm
IN_TRANSIT
   ↓ receive（部分）
PARTIALLY_RECEIVED
   ↓ receive（全部）
COMPLETED

IN_TRANSIT
   ↓ rollback
ORDERING
```

### 16.2 关键规则

- `ORDERING` 表示采购单已录入但尚未确认在途。
- 确认采购单时生成批次码，但批次尚未成为可用库存。
- `IN_TRANSIT` 表示采购货物在途。
- 可以多次收货并累计 `receivedQuantity`。
- 全部收货后进入 `COMPLETED`。
- 在途采购单可以回退到 `ORDERING`，相关预生成批次失效。

### 16.3 当前边界

采购模块已经记录预计到货日期和在途状态，但销售审批暂时不会把在途采购量计入可承诺库存。

---

## 17. 销售订单逻辑

### 17.1 状态机

```text
DRAFT
  ↓ 提交
PENDING_APPROVAL
  ├─ approve → APPROVED_AWAITING_SHIPMENT
  └─ reject  → REJECTED

APPROVED_AWAITING_SHIPMENT
  ↓ 所有出库任务完成
SHIPPED

PENDING_APPROVAL / APPROVED_AWAITING_SHIPMENT
  ├─ cancel → CANCELLED
  └─ void   → VOIDED
```

### 17.2 创建订单代表业务意图

订单创建和库存执行是分开的：

- 创建订单用于记录客户购买意图。
- 高金额或低价订单进入 `PENDING_APPROVAL`。
- 创建成功不代表已经承诺库存。
- 审批时才调用库存分配引擎。

因此，创建 99999 箱订单返回 `201 PENDING_APPROVAL` 是合理行为。

### 17.3 风控与自动审批

当前规则：

- 单价低于商品最低销售价，需要审批。
- 总金额超过配置阈值，需要审批。
- 不触发上述规则时自动进入 `APPROVED_AWAITING_SHIPMENT`。
- 自动审批订单会立即调用库存分配服务。

### 17.4 超卖拦截位置

如果订单需要人工审批：

```text
创建 99999 箱订单
        ↓
201 PENDING_APPROVAL
        ↓
经理点击 Approve
        ↓
分配引擎检查现货
        ↓
库存不足
        ↓
400 BATCH_STOCK_INSUFFICIENT
```

审批事务回滚后：

- 订单仍为 `PENDING_APPROVAL`。
- 不生成残留出库任务。
- 不扣减库存。
- 不产生负库存。

---

## 18. FEFO 与整散分离逻辑

### 18.1 分配优先级

当前智能分配顺序：

1. 用户指定批次优先。
2. 查询有效且数量大于零的批次。
3. 根据 `rejectNearExpiry` 过滤临期批次。
4. 检查总库存是否足够。
5. 按有效期升序执行 FEFO。
6. 根据箱规进行整散分配。
7. 为每个分配结果创建出库任务。

### 18.2 整散分配算法

假设箱规为 12：

```text
批次当前数量 % 12 = 该批次零散余数
批次当前数量 / 12 = 可用整箱部分
```

非整箱订单按照以下顺序处理：

1. 优先消耗各批次已有零散余数。
2. 再分配完整箱数。
3. 仍不够时拆箱补足。

整箱订单优先从完整箱数中分配。

每个批次分配结果都会生成独立 `OutboundTask`。

### 18.3 FEFO

FEFO 的核心排序：

```text
expiryDate ASC
```

有效期越近的批次越优先分配。客户设置 `rejectNearExpiry=true` 时，临期批次会先被过滤。

---

## 19. 出库确认逻辑

### 19.1 单任务确认

```text
读取 OutboundTask
        ↓
校验任务不是 COMPLETED
        ↓
校验实际出库数量
        ↓
校验绑定批次库存
        ↓
更新任务为 COMPLETED
        ↓
扣减 InventoryBatch.quantity
        ↓
库存为 0 时将批次设为 inactive
        ↓
写入库存流水
        ↓
检查销售单全部任务是否完成
        ↓
全部完成则销售单变为 SHIPPED
```

重复确认已完成任务会返回：

```text
409 OUTBOUND_TASK_ALREADY_COMPLETED
```

### 19.2 批量确认

批量接口接收任务 ID 数组：

```json
[20, 19]
```

所有任务在同一个 `@Transactional` 事务内确认。任意任务失败时，之前已在本次批量请求中处理的任务和库存扣减必须回滚。

2026-06-13 实测结果：

- 先处理待完成 Task B。
- 后处理已完成 Task A。
- Task A 触发 409。
- Task B 回滚为 `PENDING`。
- Task B `actualQty` 回滚为 0。
- 库存数量保持不变。

---

## 20. 库存数据逻辑

### 20.1 当前批次库存模型

`InventoryBatch` 保存：

- 商品。
- 批次码。
- 库位。
- 当前数量。
- 初始数量。
- 生产日期。
- 有效期。
- 实物入库时间。
- 是否有效。
- 乐观锁版本。

库存查询通常按有效批次聚合：

```sql
SUM(quantity)
WHERE product_id = ?
  AND active = true
```

### 20.2 当前库存公式

当前主要使用：

```text
现存库存 = InventoryBatch.quantity 合计
```

系统尚未形成完整的以下字段模型：

```text
Reserved Quantity  已预占数量
Available Quantity 可销售数量
Backorder Quantity 欠货数量
In-Transit ATP     可承诺在途数量
```

### 20.3 当前“预占”的真实情况

代码注释将生成出库任务描述为“库存已预占”，但当前实现只完成：

- 在 `OutboundTask` 上记录批次和计划数量。
- 不立即减少 `InventoryBatch.quantity`。
- 实际确认出库时才扣减库存。

这属于**任务级逻辑预占**，不是库存表上的**物理预占字段**。

潜在风险：

- 多张销售单可能同时读取同一批现存数量。
- 不同订单可能同时生成指向同一库存的任务。
- 后创建的订单可能到拣货确认时才发现库存不足。

因此，后续版本应优先增加真实的库存预占模型。

---

## 21. 盘点与账实差异逻辑

### 21.1 状态机

```text
CREATED
   ↓ start
COUNTING
   ↓ 全部录入或 finish
REVIEWING
   ├─ approve → COMPLETED
   └─ reject  → COUNTING
```

### 21.2 快照与盲盘

创建盘点任务时：

- 选取目标批次。
- 保存盘点快照数量。
- 创建盘点项目。

仓库人员录入实盘数量时：

```text
differenceQty = countedQty - snapshotQty
```

审核通过后：

- 盘盈增加批次库存。
- 盘亏减少批次库存。
- 记录调整前、调整后数量。
- 生成盘盈或盘亏库存流水。

### 21.3 与负库存方案的关系

当前盘点适合修正真实账实差异，但不建议把负库存作为正常销售出库方式。

如果线下确认有货而系统无货，推荐流程：

```text
紧急盘点
  ↓
确认真实批次、库位、生产日期和有效期
  ↓
主管审核盘盈
  ↓
库存入账
  ↓
销售审批与出库
```

这样可以保留食品批次追踪、成本和审计完整性。

---

## 22. 库存预测逻辑

默认使用最近 30 天出库流水：

```text
日均出库量 = 统计期内总出库量 / 统计天数

建议补货量 =
    日均出库量 × 采购提前期
    + 最低库存

预计断货天数 =
    当前库存 / 日均出库量
```

预测结果包含：

- 当前库存。
- 日均出库量。
- 建议采购量。
- 预计断货天数。
- 紧急程度。
- 预计采购成本。

注意：当前库存预测服务仍使用部分旧库存 Repository 逻辑，应与 `InventoryBatch` 单一库存数据源进一步统一。

---

## 23. 当前已验证的后端能力

### 23.1 V4.4 核心流程

已通过真实接口验证：

- JWT 登录。
- 创建仓库和库位。
- 创建商品和客户。
- 创建、审批、确认和收货入库单。
- 双批次库存生成。
- 销售订单创建。
- FEFO 分配。
- 整散分离。
- 两个出库任务确认。
- 库存从 150 扣减为 85。
- 销售订单最终变为 `SHIPPED`。

### 23.2 防御性测试

| 测试 | 结果 |
|---|---|
| 批量确认事务回滚 | 通过 |
| 重复确认防双击 | 通过，返回 409 |
| 超卖审批拦截 | 通过，返回 400 |
| 超卖失败无残留任务 | 通过 |
| 库存不出现负数 | 通过 |
| 无 Token 创建仓库 | 通过，返回 403 |
| 重复销售审批状态冲突 | 通过，返回 409 |

### 23.3 IAM 与逻辑删除验证

| 测试 | 预期 | 结果 |
|---|---|---|
| 创建不依赖旧 `role` 字段的用户 | 201 | 通过 |
| 当前用户删除自己 | 403 | 通过 |
| 删除最后一个有效 `SUPER_ADMIN` | 拒绝操作 | 通过 |
| 管理员删除普通用户 | 204 | 通过 |
| 被删除用户旧 JWT 继续访问 | 401 或 403 | 通过，返回 403 |
| 删除后用户主体仍保留 | `is_deleted = true` | 通过 |
| 删除后账号被禁用 | `enabled = false` | 通过 |
| 删除后角色关联清空 | `role_count = 0` | 通过 |
| IAM 单元测试 | 全部通过 | 22/22 通过 |

数据库抽查结果：

```text
is_deleted = true
enabled = false
role_count = 0
username = deleted_<userId>_<randomKey>
```

### 23.4 全路由回归结果

2026-06-14 完成后端公开 API 全量回归：

| 指标 | 结果 |
|---|---:|
| 预期接口路由 | 97 |
| 实际覆盖路由 | 97 |
| 缺失路由 | 0 |
| 测试执行次数 | 112 |
| 通过次数 | 112 |
| 失败次数 | 0 |

回归报告：

```text
D:\ERP_WMS\BC warehouse\2G\target\codex-runtime\
all-api-verification-1781435149047.json
```

---

## 24. 当前已知问题和边界

### 24.1 已解决：重复审批错误映射

对已经自动审批的销售单再次调用审批：

```text
409 SALES_ORDER_INVALID_STATUS
```

该问题已经修复。状态机仍然拒绝重复审批，但错误已规范映射为 `409 Conflict`，不会再产生误导性的 `500 Internal Server Error`。

### 24.2 缺少真实库存预占

当前出库任务只表达逻辑分配，没有独立的 `reservedQuantity`。这是下一阶段最高优先级的库存一致性问题。

### 24.3 尚未实现 Backorder

当前库存不足时审批整体失败，不支持：

- 先分配现有库存。
- 剩余数量转欠货。
- 入库后自动唤醒欠货。

### 24.4 尚未实现销售与在途采购联动

采购单存在 `IN_TRANSIT` 和预计到货日期，但销售审批暂时只检查已入库现货。

### 24.5 不支持受控负库存履约

当前库存扣减会检查批次可用量，不允许正常业务产生负库存。这符合批次食品 WMS 的审计要求。

### 24.6 库存数据源仍有历史双轨

系统设计声明 `InventoryBatch` 是库存单一事实来源，但部分预测和旧库存服务仍引用 `Inventory` Repository。后续应统一，避免统计口径不一致。

### 24.7 配置安全

开发配置文件中存在数据库密码和 JWT Secret 默认值。生产部署必须改为环境变量或密钥管理服务，不得使用仓库内默认值。

### 24.8 IAM 多节点会话失效

当前单实例通过“每次请求重新加载有效用户 + 权限缓存清理”实现删除后旧 Token 失效。

如果未来部署多个后端实例，需要进一步建设：

- Redis Token 黑名单；或
- 用户级 `token_version`；或
- 集中式会话/身份服务。

否则，一个节点删除用户后，其他节点的本地权限缓存可能存在短暂不一致窗口。

### 24.9 数据库迁移部署

本次 IAM 改造提供迁移文件：

```text
src/main/resources/db/migration/
V4_7__Enterprise_IAM_Soft_Delete.sql
```

该脚本负责：

- 取消旧 `users.role` 的非空和检查约束。
- 新增 `is_deleted`、`deleted_at`、`deleted_by`。
- 新增活动用户和删除时间索引。

当前工程的 Flyway 自动执行链路尚未完成生产基线化。部署到新环境前，必须由发布流程确认迁移脚本已执行，不能只发布 Java 程序而遗漏数据库字段。

---

## 25. 推荐后续开发顺序

### P0：库存预占

增加批次级预占能力：

```text
onHandQuantity
reservedQuantity
availableQuantity = onHandQuantity - reservedQuantity
```

审批时增加预占，取消订单时释放预占，确认出库时把预占转换为实际扣减。

### P1：Backorder

库存不足时允许：

```text
订单数量 100
现货 10

已分配 10
欠货 90
履约状态 PARTIALLY_ALLOCATED
```

新库存入库后自动尝试补分配欠货数量。

### P2：在途库存承诺

建立：

```text
ATP =
现货可用库存
+ 交付日期前可信在途库存
- 已承诺需求
```

在途货物只能形成供应承诺，实物收货前不能生成真实批次拣货任务。

### P3：紧急库存校正

为主管提供受控的“紧急盘点/快速盘盈”流程，代替直接负库存出库：

- 必须填写原因。
- 必须指定真实批次和库位。
- 必须填写有效期等食品追踪字段。
- 必须双人审批。
- 必须生成审计流水。

### P4：接口和统计统一

- `SALES_ORDER_INVALID_STATUS` 映射为 409 已完成。
- 统一库存总数接口。
- 统一库存预测的数据源为 `InventoryBatch`。
- 清理重复或历史库存模型。
- 为安全、并发、事务和权限建立持续回归测试。

### P5：IAM 多节点增强

- 引入 Redis Token 黑名单或用户级 `token_version`。
- 增加用户恢复、冻结、解冻和强制退出接口。
- 将高风险管理员操作写入独立安全审计日志。
- 为删除最后一个 `SUPER_ADMIN`、角色清空和跨节点 Token 失效建立持续集成测试。
- 完成 Flyway 基线化和生产自动迁移流程。

---

## 26. 推荐目标模型

建议将商业订单状态和履约状态分离。

商业状态：

```text
DRAFT
PENDING_APPROVAL
APPROVED
REJECTED
CANCELLED
VOIDED
```

履约状态：

```text
UNALLOCATED
PARTIALLY_ALLOCATED
ALLOCATED
WAITING_INBOUND
BACKORDERED
PICKING
SHIPPED
```

库存口径：

```text
On Hand   = 实际在库数量
Reserved  = 已承诺但未发货数量
Available = On Hand - Reserved
In Transit = 已确认采购但未收货数量
ATP       = Available + 交期内可信 In Transit - 其他承诺
```

推荐总体方案：

> 以 Backorder 作为缺货主流程，以在途库存承诺增强交付预测，以受控紧急盘点代替负库存强制发货。

---

## 27. 结论

当前后端已经具备较完整的 WMS 基础能力：

- 主数据。
- 多仓多库位。
- 采购和入库。
- 批次库存。
- 销售审批。
- FEFO。
- 整散分离。
- 出库任务。
- 库存流水。
- 盘点平账。
- 企业级 IAM、动态 RBAC 和用户逻辑删除。
- 库存预测。
- Shopify 同步基础。

V4.4 核心业务链路、主要防御性测试以及 IAM 安全回归已经通过。当前全量验证覆盖 97 个接口路由、执行 112 次测试，结果全部通过。

下一阶段不应优先开放负库存，而应先解决真实库存预占，再建设 Backorder 和在途库存承诺。这样才能在支持业务灵活性的同时，守住批次追踪、库存一致性和财务审计底线。
